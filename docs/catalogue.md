# FixtureShark data catalogue

Generated from `public.meta_flow_nodes` by `scripts/export_catalogue.py`. **Do not edit here**: change entries in the database (Data Flow page, or an UPDATE); the weekly export brings this file back in line.

264 live objects, 264 documented, 98 marked usable by AI tools. Objects whose definition changes after their entry was reviewed appear in `meta_catalogue_gaps` and as the `catalogue_current` warning in the daily integrity run.

Columns: **Status** current / superseded / experimental / unused. **Public**: the public site reads it. **AI**: may back a read-only AI Lab tool. Methodology for the models is in `docs/methodology/`.

## Contents

- [source](#source) (14)
- [pipeline](#pipeline) (29)
- [model](#model) (36)
- [fantasy](#fantasy) (36)
- [finance](#finance) (11)
- [broadcast](#broadcast) (11)
- [api](#api) (60)
- [helper](#helper) (14)
- [admin](#admin) (22)
- [meta](#meta) (5)
- [scratch](#scratch) (26)

## source

Raw and reference data as it arrives: results, fixtures, FPL, teams, leagues.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `countries` | table | current | yes | yes | Reference list of countries (one row per country: name, code) that leagues and teams belong to. Used for country filters on Results Data / League Table / Team Strength, the Country Insights page (/football/countries-compared) and the TV-guide views. | Static reference data; rows added by migrations when new countries/leagues are onboarded. |
| `fixtures` | table | current | yes | yes | FixtureShark's canonical fixture list for the current season across tracked competitions: one row per match (league, season, home/away team, kickoff date/time, matchweek/round, status, slug) carrying the model's pre-match Dixon-Coles expected goals (predicted_home/away_goals; raw_predicted_* exclude manual overrides; prediction_fit_run_id and prediction_model_version identify the fit). The main source for fixture, match-preview, team, Fantasy and landing pages. Caveats: predicted_* are expected goals (lambdas), not scorelines; there are no score columns (results are in matches); status can say 'played' just because kick-off passed, and can lag the other way; played-fixture predictions are frozen from the last fit before kick-off. | Kick-off updates by refresh_fixture_feeds() (pg_cron 15 4 * * *); new EC fixtures inserted by scripts/sync-fixtures.ts in the 'Daily Premier League Import' workflow (0 6 * * *); cup fixtures upserted by ingest-cup-data (pg_cron 15 5 * * *); status set to played from results by sync_fixture_status_from_results (pg_cron 30 9,21 * * *); predictions by backfill_fixture_predictions (04:15 cron, 06:00 workflow, cup ingest). |
| `leagues` | table | current | yes | yes | Reference list of competitions: one row per league or cup with code (football-data.co.uk style, e.g. E0 Premier League, E1 Championship, EC National League, D1, I1, SP1, UCL, LC, FAC), name, slug, country, tier, competition_type, scope and confederation. Used everywhere to label and filter matches, fixtures and model fits. | Static reference data maintained by migrations/manual SQL (e.g. 20260924120000_la_liga_league_row.sql); importers look leagues up and never create them. |
| `match_odds` | table | current | yes | yes | Bookmaker odds for archived matches: one row per (match_id, source_name, market, bookmaker, is_closing), markets '1x2' (price_home/draw/away), 'ou25' (price_over/under, line 2.5) and 'ah' (Asian handicap line with home/away prices), from football-data.co.uk. Read by the public Model Returns, market-efficiency and scorecard features (get_betting_bets, get_market_efficiency, get_overround_trend, model_scorecard_matches). Caveats: bookmaker values 'Max' and 'Avg' (and 'P' on some markets) are market summaries, not real bookmakers; decimal odds include the bookmaker margin; linked to matches, not fixtures. | Populated only by backfill_match_odds(), run manually after raw CSV rows are loaded into source_match_rows (backfill-football-raw edge function); no scheduled refresh, so recent matches may lack odds. |
| `matches` | table | current | yes | yes | The results archive: one row per completed match (league, season, date, teams, full-time and half-time score, result H/D/A, shots, corners, fouls, cards, referee, and where available xG, attendance and penalty-shootout outcome). It is what the Dixon-Coles model is fitted on and what results, league-table, team, model-accuracy and Beat the Shark pages read. Caveats: no predictions here (current-season predictions are on fixtures, archived-match predictions in match_predictions); upcoming games are in fixtures; stats columns are null for sources that do not supply them. | Upserted daily by scripts/import-daily.ts in 'Daily Premier League Import' (0 6 * * *, E0-E3, EC) and 'Daily International Import' (20 6 * * *, non-English top flights); cups by the ingest-cup-data edge function (pg_cron 15 5 * * *); past seasons via the manual 'Import a historic season' workflow. Row estimate 0 in stats is unreliable. |
| `opta_slot_breakdown` | table | current | yes | yes | Opta 2011/12 Premier League output aggregated by formation code + slot (starts, minutes, goals and assists split by open play/corners/free kicks/penalties, key passes, shots). Powers the FPL Set Pieces page and get_set_piece_index(). Historic 2011/12 sample used as role priors — not current-season stats. | One-off import: scripts/extract_opta_workbook.py prints SQL, applied manually. |
| `point_deductions` | table | current | yes | yes | Manual points adjustments (deductions or corrections): one row per league + season + team adjustment with reason and date. Applied on top of match results in the League Table (getLeagueTable/getPointsRace) and the league_standings view. | Manual SQL when a deduction is announced. |
| `raw_match_files` | table | current | no | no | Archive of every raw source file ingested: one row per file (source, URL, competition, season, content hash, row count, column names). Parent of source_match_rows; browsed on the admin Source Data page. | Written by edge functions backfill-football-raw (football-data.co.uk CSVs) and backfill-cup-raw (footballwebpages cup HTML), invoked manually — not in pg_cron. |
| `seasons` | table | current | yes | yes | Reference list of seasons: one row per season with label in 'YYZZ' form (e.g. '2627' = 2026/27), start_year, end_year and slug. season_id is used by every table; season_id 13 is 2026/27 and 9-12 are 2022/23-2025/26. Several pipelines hard-code the current label or id. | Static reference data maintained by hand/migrations; importers look seasons up by label and never create them. |
| `source_match_rows` | table | current | no | no | Every row of every raw source file, verbatim as JSON (raw_data), one row per file + source row key. Audit/provenance layer beneath matches; also read by backfill_match_odds() to recover historic odds. Keys in raw_data differ per file — check raw_match_files.column_names. | Written by edge functions backfill-football-raw and backfill-cup-raw (manual invocation). |
| `tactical_data_sources` | table | current | yes | no | Registry of external tactical datasets (source name, season label, competition, provider, date range); one row per dataset. Dimension for tactical_formation_slot_aggregates via the tactical_formation_slot_priors view. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `tactical_formation_mappings` | table | current | yes | no | Maps each source dataset's formation codes to canonical formations with confidence/method; one row per source + formation code. Joined in tactical_formation_slot_priors. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `tactical_formation_slot_aggregates` | table | current | yes | no | Historic output totals per source dataset + formation code + slot + venue scope (starts, minutes, goals, assists, shots, big chances...). Turned into per-90 rates and shares by tactical_formation_slot_priors for the FPL Formations page. Historic priors, not current-season stats. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `teams` | table | current | yes | yes | Reference list of clubs: one row per team with country, canonical_name (unique per country), display_name for the UI and slug for URLs (slug changes are logged in team_slug_history by trigger). team_id is FixtureShark's id; source spellings map to it via team_aliases, and FPL team ids map via fpl_teams.canonical_team_id. Used by almost every page, the model and finance. | Static reference data maintained by hand/migrations (e.g. display-name clean-up in 20260925180000_fixture_feeds_sc0_d1.sql); imports resolve names through team_aliases and do not create teams. |

## pipeline

Jobs, run logs and intermediate objects that move data between layers.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `fixture_changes` | table | current | yes | yes | Log of detected kickoff date/time changes: one row per change to a fixture (old vs new date/time, detected_at). Read by the site-wide FixtureChangeBanner, which only shows Premier League (E0) changes from the last 7 days; covers every refreshed competition in the table itself. | Written by public.refresh_fixture_feeds() (pg_cron refresh-football-fixtures-daily, 04:15 UTC daily) and scripts/sync-fixtures.ts (Daily Premier League Import workflow, 06:00 UTC). |
| `fixture_player_lineup_consensus` | view | current | no | no | Weighted vote of active external predicted-lineup sources: one row per fixture x team x FPL player on that team, with the weighted share of sources predicting him to start (source_consensus_probability), availability from FPL status/chance_of_playing, and the most common predicted tactical role. Only fixtures with at least one lineup prediction appear. First step of the live expected-minutes chain (feeds fixture_player_expected_minutes_v2; also the superseded v1). Joins fpl_players on canonical_team_id with no season filter. Prediction consensus, not confirmed lineups. |  |
| `fixture_player_tactical_consensus` | view | current | no | no | Predicted tactical role per player per fixture: one row per fixture x team x current-season FPL player, taking the highest-weighted role across active lineup-prediction sources, or else the team's tactical default (team_player_tactical_defaults) or a generic GK/DEF/MID/CF role from FPL position (sources = 0). Used by the live projection chain (fixture_player_expected_minutes_resolved, refresh_fpl_projection_fixture_v6_impl) and fpl_season_player_projection_feed; the public fixture page now takes tactical_role from fpl_projection_frontend_feed_v6. Model/prediction input, not observed. |  |
| `fixture_refresh_runs` | table | current | yes | no | Run log for the fixture-feed refresh: one row per run (competitions, rows seen/updated, status, error). Used for the 'data last refreshed' stamp on the gameweek browser, the admin Data Health page and the data_health view. Operational log, not football data. | Written by public.refresh_fixture_feeds() (pg_cron refresh-football-fixtures-daily, 04:15 UTC) and scripts/sync-fixtures.ts (Daily Premier League Import, 06:00 UTC). |
| `fixture_team_tactical_consensus` | view | current | yes | yes | Predicted formation per team per fixture (e.g. 4-2-3-1) with a consensus weight and source count: a manual formation from team_tactical_defaults takes precedence, then the highest-weighted scraped lineup-source formation, then a non-manual default. Read by the public FPL fixture projection page (/fpl/fixture/:id) and the tactical-roles admin page. Caveat: the manual and default branches only emit rows for fixtures that have 'leaguewide_v4' rows in fpl_player_projections (v4 is retired), so for newer fixtures manual/default formations may be missing and only scraped consensus appears. Prediction, not observed. |  |
| `match_import_runs` | table | current | no | no | Run log for the daily results import: one row per league import attempt (league_code, rows seen/upserted, status, error). Shown on the admin Data Health page. Operational log only. | Written by scripts/import-daily.ts — GitHub workflows Daily Premier League Import (06:00 UTC, E0–E3, EC), Daily International Import (06:20 UTC, 18 non-English leagues) and manual Import a historic season. |
| `pipeline_runs` | table | current | no | no | Run log for Python pipeline jobs: one row per job run (job_name, status running/success/warning/failed, summary, error). Covers FPL projections, fixture bonus and final-table simulations, integrity checks and Netlify usage; shown on the admin Data Health page. | Written by scripts/refresh_fpl_projections.py, simulate_fixture_bonus.py, simulate_final_table.py (FPL Projections Pipeline, 06:41 and 18:41 UTC), run_integrity_checks.py (09:45 UTC) and netlify_usage_estimate.py (05:30 UTC). |
| `result_ingestion_runs` | table | current | no | no | Run log for result-ingestion edge functions: one row per run (source, competitions attempted, rows seen/stored, matches inserted/changed, unmatched rows, status, details). Read by the data_health view and check_model_integrity(). Rows from 11–17 Sep 2026 are junk from the retired ingest-football-results stub. | Written by edge function ingest-cup-data (pg_cron ingest-cup-data-daily, 05:15 UTC). |
| `team_aliases` | table | current | no | no | Maps raw team names used by each external source (football-data.co.uk, FootballWebPages, broadcasters...) to team_id; one row per source + raw name. Used by every importer to resolve team names; unmapped names are dropped (see get_unmapped_alias_names()). | Manual/migration inserts when a new source name appears. |
| `backfill_fixture_predictions()` | function | current | no | no | Writes Dixon-Coles expected goals onto every scheduled or postponed fixture from each league's latest accepted fit (team_ratings), applying manual team-strength overrides and, for the Premier League (league_id 1), sparse-data shrinkage and home/away adjustments (model dc_home_away_sparse_shrink_v2; others dc_baseline_v1). Sets predicted_*, raw_predicted_* (without overrides), prediction_fit_run_id, prediction_model_version and predicted_at; returns rows updated. Kick-off guard: only uses fits dated before the fixture date, and never touches played fixtures. | Called by refresh_fixture_feeds() (pg_cron refresh-football-fixtures-daily, 15 4 * * *); by scripts/refresh-predictions.ts in the 'Daily Premier League Import' workflow (0 6 * * *) after refits; by the ingest-cup-data edge function (pg_cron ingest-cup-data-daily, 15 5 * * *); and by the admin team-ratings page after saving an override. |
| `backfill_historic_fixture_predictions(target_season_id bigint)` | function | superseded | no | no | One-off backfill that fills predicted_home/away_goals on a season's fixtures that have none, each from the newest accepted fit dated before kick-off (point-in-time). Uses the plain baseline formula (no PL shrinkage, home/away adjustment or overrides) and does not set prediction_model_version or raw_predicted_*, so its rows are not directly comparable with backfill_fixture_predictions output. | Manual only: no caller in scripts, workflows, cron or src (only comments). Archived-match predictions are now produced by backfill_match_predictions into match_predictions. |
| `backfill_match_odds()` | function | current | no | no | Unpivots bookmaker prices held in raw football-data.co.uk rows (source_match_rows.raw_data) into match_odds: 1X2 (B365, BW, IW, PS, WH, VC, Max, Avg), over/under 2.5 and Asian handicap, opening and closing, matched to matches via team_aliases. Insert-only (ON CONFLICT DO NOTHING), so corrected prices are never updated; returns rows inserted. | Manual only: no caller in cron, workflows, scripts or edge functions. The daily importer writes matches but not source_match_rows, so match_odds only grows when source_match_rows is reloaded (backfill-football-raw edge function) and this is run by hand. |
| `backfill_match_predictions(p_league_id bigint, p_season_id bigint)` | function | current | no | no | Writes point-in-time Dixon-Coles expected goals for archived matches of one league and season into match_predictions: each match takes the newest accepted fit dated strictly before the match that rates both teams, replacing any older choice on rerun (converges). Returns rows inserted or changed. | Called by scripts/retrofit_verify_and_predict.py in the manual 'retrofit-season' GitHub workflow, after retro-fits are produced. |
| `get_fpl_played_matchweeks(p_season_id bigint, p_league_id bigint)` | function | current | no | no | Returns an integer array of matchweeks that have actual FPL gameweek rows (fpl_player_gameweeks joined to fixtures) for a season and league. Pipeline helper for the hindsight-optimal solver. | Called by scripts/solve-hindsight-optimal.ts in the hindsight-optimal workflow (daily 06:45 UTC). |
| `get_fpl_projection_available_matchweeks(p_season_id bigint, p_league_id bigint, p_model_version text, p_scenario_key text)` | function | current | no | no | Returns an integer array of matchweeks that already have fpl_player_projections for a season/league/model_version/scenario. Pipeline helper used to find gaps before and after a projection backfill. | Called by scripts/backfill-projections.ts in the backfill-projections workflow (manual dispatch). |
| `import_fpl_gameweeks(p_season_id bigint, p_folder text)` | function | current | no | no | Imports one past season's per-gameweek player history from vaastav/Fantasy-Premier-League (data/<folder>/gws/merged_gw.csv) into fpl_player_gameweek_history, resolving element ids to fpl_code via fpl_player_season_totals (which must be imported first). Validates the header, skips malformed rows but aborts if more than 1% (min 5) are bad; insert-only. Returns counts of imported, malformed and unmatched rows. | Manual, one-off per historic season (service_role only); not scheduled. |
| `import_fpl_season(p_season_id bigint, p_folder text)` | function | current | no | no | Imports one past season's player totals from vaastav/Fantasy-Premier-League (data/<folder>/players_raw.csv) into fpl_player_season_totals (players with minutes > 0 only; start_cost derived as now_cost minus cost_change_start) and widens player_identity's first/last-seen seasons. Resolves columns by name because the CSV layout changes by season; aborts on more than 1% malformed rows; insert-only. | Manual, one-off per historic season (service_role only); not scheduled. |
| `refresh_feed_nearest_date(p_code text, p_url text)` | function | current | no | no | Generic fixture-feed loader for fixturedownload.com JSON feeds: for league p_code, matches feed rows to existing fixtures by teams and nearest date (within 60 days), logs date/time changes to fixture_changes, updates kickoff date/time/matchweek/status, and inserts missing fixtures. Returns the number of rows changed. Caveats: hard-coded to season label '2627' (will silently do nothing next season); status is set to 'played' purely because kick-off time has passed, not from results; unmatched team names are dropped silently. | Called by refresh_fixture_feeds() (pg_cron 'refresh-football-fixtures-daily', 04:15 UTC) for I1, F1, SP1, and via refresh_scottish_premiership_fixtures() for SC0. |
| `refresh_fixture_feeds()` | function | current | no | no | Daily fixture-list sync from fixturedownload.com JSON feeds for E0-E3, UCL, UEL, UECL and D1 (plus SC0, I1, F1, SP1 via isolated sub-functions): logs kick-off changes to fixture_changes, updates kickoff date/time, matchweek and status on existing fixtures (it never inserts new ones), records the run in fixture_refresh_runs, then calls backfill_fixture_predictions(). Caveats: status becomes 'played' purely because kick-off time has passed, not because a result exists; feed URLs and season label '2627' are hard-coded to 2026/27; docs/incidents.md records that it reports success while matching far fewer rows than the feed contains. | pg_cron job refresh-football-fixtures-daily (15 4 * * *, 04:15 UTC). |
| `refresh_fpl()` | function | current | no | no | Thin public wrapper that just runs private.refresh_fpl(), the full official-FPL-API sync (fpl_teams, fpl_players, fpl_gameweeks, fpl_fixtures, fpl_player_gameweeks, fpl_player_snapshots, logged in fpl_ingestion_runs). Kept so manual runs hit the same body cron uses; the two copies diverged once and caused an outage (OUTSTANDING.md). | Manual/service_role only; pg_cron job fpl-refresh-6-hourly (17 */6 * * *) calls private.refresh_fpl() directly, not this wrapper. |
| `refresh_fpl_bonus_v3_for_fixture(p_fixture_id bigint)` | function | superseded | no | no | Older closed-form (Plackett-Luce style) FPL bonus estimate for one fixture: scores expected BPS from season BPS/minute rates, xG, xA, clean sheets and defensive contributions, converts to probabilities of 1st/2nd/3rd, and overwrites expected_bonus/xpts_bonus/expected_fpl_points on the leaguewide_v6 baseline rows in fpl_player_projections. | No caller anywhere in the repo, cron or definitions. Superseded by the Monte Carlo bonus (fpl_fixture_bonus_montecarlo_v1) and the get_fpl_fixture_bonus_v4 fallback used inside refresh_fpl_projection_fixture_v6_impl; running it would be overwritten by the next projection refresh. |
| `refresh_fpl_projection_fixture_v6(p_fixture_id bigint, p_allow_played boolean)` | function | current | no | no | Public entry point to refresh FPL projections for one fixture: returns 0 without writing if the fixture is already played (unless p_allow_played = true), otherwise calls refresh_fpl_projection_fixture_v6_impl. Returns rows written. | Called once per fixture by scripts/refresh_fpl_projections.py in the 'FPL Projections Pipeline' workflow (41 6 * * * and 41 18 * * * UTC, two passes) and the manual 'Refresh FPL Projections' workflow (also triggered from the Team Strength page button); scripts/backfill-projections.ts ('Backfill Projections' workflow, manual) calls it with p_allow_played = true; also looped by refresh_fpl_projections_range. |
| `refresh_fpl_projection_fixture_v6_impl(p_fixture_id bigint)` | function | current | no | no | Core writer of FPL projections for one fixture: reads fpl_projection_leaguewide_final plus secondary scoring, resolved expected minutes, tactical roles and bonus (Monte Carlo table if present, else get_fpl_fixture_bonus_v4), and upserts model_version 'leaguewide_v6' rows into fpl_player_projections, returning rows written. Has no played-fixture guard of its own. | Internal: called only by refresh_fpl_projection_fixture_v6 (which adds the played-fixture guard); should not be called directly. |
| `refresh_fpl_projections_range(p_from_matchweek integer, p_to_matchweek integer, p_season_id bigint, p_league_id bigint)` | function | superseded | no | no | Loops over every fixture in a matchweek range (defaults season_id 13, league_id 1) and calls refresh_fpl_projection_fixture_v6 for each, skipping played fixtures; returns the number of fixtures visited (not rows written). Over PostgREST it exceeds the gateway/statement timeout (~85s for 10 gameweeks), which is why callers moved to per-fixture calls. | No live caller: the Team Strength page and scripts/refresh_fpl_projections.py stopped calling it (per their comments) and now loop per fixture; still executable by service_role for direct SQL use. Hard-coded default season 13 (2026/27). |
| `refresh_model_scorecard()` | function | current | no | no | Refreshes the model_scorecard_matches materialized view (concurrently) and returns its row count. | pg_cron 'refresh-model-scorecard' at 10:00 and 22:00 UTC daily. |
| `refresh_scottish_premiership_fixtures()` | function | current | no | no | Thin wrapper that loads the Scottish Premiership (SC0) 2026 fixturedownload.com feed via refresh_feed_nearest_date. Feed URL is hard-coded to the 2026 season. | Called by refresh_fixture_feeds() (pg_cron 'refresh-football-fixtures-daily', 04:15 UTC). |
| `snapshot_due_fpl_projections()` | function | current | no | no | Scheduler for FPL projection snapshots: for gameweeks whose deadline is between 2 days ago and 60 minutes ahead and that have no 'pre_deadline'/'late' snapshot yet, takes a 'pre_deadline' snapshot (if before the deadline) or a 'late' one (if the deadline passed but no match has kicked off). Returns a JSON list of what was captured. | pg_cron 'snapshot-fpl-projections' every 10 minutes. |
| `snapshot_fpl_projections(p_season_id bigint, p_event integer, p_kind text)` | function | current | no | no | Copies the current fpl_player_projections (all model versions) and the fixtures' predicted goals for one FPL gameweek into the append-only fpl_projection_snapshots / fpl_projection_fixture_snapshots tables, tagged with snapshot_kind ('pre_deadline', 'late' or 'manual'). Idempotent (on conflict do nothing). Returns counts. | Called by snapshot_due_fpl_projections() (pg_cron every 10 min); can be run manually with kind 'manual' (e.g. GW6 baseline taken 26 Sep 2026). |
| `sync_fixture_status_from_results()` | function | current | no | no | Marks fixtures 'played' when they are still 'scheduled'/'postponed', kicked off before today, and a matching result (same teams and date, goals present) exists in matches. Returns rows updated. Independent of the fixture feeds, so stale statuses get fixed even if a feed fails. | pg_cron 'sync-fixture-status-from-results' at 09:30 and 21:30 UTC daily. |

## model

Dixon-Coles match model, its fits, predictions, scorecards and inputs.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `fixture_player_expected_minutes_fallback` | view | current | no | no | History-based fallback expected minutes for EVERY current-season FPL player in every Premier League (E0) fixture of the current FPL season: one row per fixture x team x player, with start probability from this season's starts/appearances (or squad status when there is no history), sub probability, availability from FPL status/chance_of_playing, and minutes scaled to 990 per team; minutes_source = 'fallback_history_hierarchy'. Base row set of fixture_player_expected_minutes_resolved (live FPL projection chain). Model estimate, not observed; internal input. |  |
| `fixture_player_expected_minutes_resolved` | view | current | no | no | Merges lineup-consensus minutes (fixture_player_expected_minutes_v3) over the history fallback: one row per E0 fixture x team x current-season FPL player, taking v3 values where the player appears in predicted lineups (minutes_source 'lineup_consensus_v3') and fallback values otherwise, with tactical role from fixture_player_tactical_consensus. Intermediate step of the live chain (feeds fixture_player_expected_minutes_resolved_v3_raw); also read by the superseded fpl_projection_frontend_feed. Model estimate; internal. |  |
| `fixture_player_expected_minutes_resolved_v3` | view | current | no | no | Final expected-minutes layer of the live FPL projection model (leaguewide_v6): one row per E0 fixture x team x current-season FPL player with expected_minutes, prob_starting_xi, prob_sub_appearance, availability_probability, tactical_role and minutes_source. Applies per-team team-sheet limits to fixture_player_expected_minutes_resolved_v3_raw (one lead goalkeeper keeps his minutes; outfield minutes/starters trimmed to 900/10) since 26 Sep 2026. Feeds fpl_projection_v4_leaguewide_inputs, fpl_set_piece_fixture_exposure_v1 and refresh_fpl_projection_fixture_v6_impl. Computed on read from current inputs, so it can differ from the minutes stored in fpl_player_projections; model estimate, not observed. Despite the 'v3' name it is the live layer. |  |
| `fixture_player_expected_minutes_v2` | view | current | no | no | Second-generation lineup-consensus minutes: one row per fixture x team x FPL player present in fixture_player_lineup_consensus (fixtures that have scraped lineup predictions), with start probability from source consensus x availability, sub probability from this season's substitution history and squad status, and minutes redistributed so a team sums to 990. Not used directly by any page; it is the input to fixture_player_expected_minutes_v3, so it is part of the live chain. Model estimate; internal. |  |
| `fixture_player_expected_minutes_v3` | view | current | no | no | Lineup-consensus expected minutes with substitute appearances normalised to ~4.25 per team per match: one row per fixture x team x FPL player for fixtures with lineup predictions, built from fixture_player_expected_minutes_v2. Overrides the history fallback in fixture_player_expected_minutes_resolved and resolved_v3_raw (live chain). Model estimate; internal. |  |
| `fpl_defensive_contribution_projection_leaguewide` | view | current | no | no | Probability each outfield player reaches the FPL defensive-contribution threshold (10 CBIT for defenders, 12 CBIRT for midfielders/forwards) in a fixture: one row per fixture x team x player from fpl_projection_leaguewide_allocation_v2, using this season's CBI/recoveries/tackles per 90 (shrunk to a position prior) scaled by start probability and expected minutes; goalkeepers get 0. Internal component of the live leaguewide_v6 projection (feeds fpl_projection_leaguewide_points, get_season_player_projections_json, refresh_fpl_bonus_v3_for_fixture). Model estimate. |  |
| `fpl_fallback_start_probability_v6` | view | current | no | no | History-only start probability for every current-season FPL player in every current-season E0 fixture: one row per fixture x team x player with start_probability (tiers for 'nailed'/'strong' recent start records, otherwise a smoothed starts/appearances rate or squad-status prior, times availability), apps/starts/subs counts and probability_source. Used by fixture_player_expected_minutes_resolved_v3_raw to override start chances for players not in predicted lineups (minutes_source nailed_history_v6 / strong_history_v6 / fallback_history_v6). 'Starts' are inferred as 60+ minute appearances. Model estimate; internal. |  |
| `fpl_fixture_bps_projection_v1` | view | current | no | no | Expected FPL bonus-points-system (BPS) score per player per fixture: one row per fixture x team x player with expected_bps_score, within-fixture bps_rank and a deterministic 3/2/1 bonus, built from fpl_projection_leaguewide_allocation_v2 goals/assists, clean-sheet chance, and shrunk BPS/CBI/recovery rates. Input to the Monte Carlo bonus simulation (scripts/simulate_fixture_bonus.py, run by the FPL Projections Pipeline workflow) and to get_fpl_fixture_bonus_v4 (closed-form fallback used by refresh_fpl_projection_fixture_v6_impl when no Monte Carlo result exists). Internal model input, not published bonus. |  |
| `fpl_projection_leaguewide_allocation_v2` | view | current | no | no | Core attacking allocation of the live leaguewide_v6 model: one row per fixture x team x player splitting the team's predicted goals (fixtures.predicted_home/away_goals, Dixon-Coles) into player expected_goals and 72% of it into expected_assists, using shrunk season xG/xA per 90, tactical-role weights, expected minutes and penalty/free-kick/set-piece exposure. Feeds fpl_projection_leaguewide_points, secondary scoring, defensive contribution, BPS and bonus functions. Only fixtures with team goal predictions appear. Model estimate; internal. Despite the 'v2' name it is in the live chain. |  |
| `fpl_projection_leaguewide_final` | view | current | no | no | Pre-bonus full expected-points breakdown per fixture x player: joins fpl_projection_leaguewide_points with fpl_projection_secondary_scoring (saves, goals conceded, cards/own goals, penalties) and sums expected_fpl_points. Read directly by refresh_fpl_projection_fixture_v6_impl, which adds Monte Carlo or closed-form bonus and writes fpl_player_projections (leaguewide_v6). Its expected_bonus/xpts_bonus are always 0 and it is computed live, so its totals differ from the published projections; use fpl_projection_frontend_feed_v6 instead. Internal. |  |
| `fpl_projection_leaguewide_points` | view | current | no | no | Primary expected-points components per fixture x player: appearance, goals, assists, clean sheet (Poisson P(0) of opponent's predicted goals x start probability) and defensive-contribution points, from fpl_projection_leaguewide_allocation_v2 and fpl_defensive_contribution_projection_leaguewide; bonus is 0 here. Input to fpl_projection_leaguewide_final (live chain). Model estimate; internal. |  |
| `fpl_projection_secondary_scoring` | view | current | no | no | Secondary expected-points components per fixture x player: goalkeeper save points, goals-conceded deductions (GK/DEF), card and own-goal deductions, and penalty save/miss points, from this season's rates (shrunk to priors) scaled by expected minutes and the opponent's predicted goals. Joined in fpl_projection_leaguewide_final and refresh_fpl_projection_fixture_v6_impl (live chain). Model estimate; internal. |  |
| `fpl_projection_v4_leaguewide_inputs` | view | current | no | no | Player inputs for the leaguewide goal/assist allocation: one row per current-season fixture x team x player from fixture_player_expected_minutes_resolved_v3 (minutes, start/sub/availability probabilities, tactical role) plus season minutes and raw xG/xA per 90 (xG finishing-adjusted) from fpl_players, restricted to fixtures with team goal predictions. Feeds fpl_projection_leaguewide_allocation_v2. Despite the 'v4' name it is in the live leaguewide_v6 chain. Internal. |  |
| `fpl_set_piece_fixture_adjustments_v1` | view | current | no | no | Per fixture x team x player penalty, direct free-kick and set-piece-creation exposure shares (0-1, normalised per team when they sum above 1), derived from set-piece hierarchy rank weighted by start probability and expected minutes (via fpl_set_piece_fixture_exposure_v1). Used by fpl_projection_leaguewide_allocation_v2 to tilt goal/assist shares (live chain). Model input; internal. |  |
| `fpl_set_piece_fixture_exposure_v1` | view | current | no | no | Links each player's set-piece duties (set_piece_hierarchies: type and rank for the current season) to his per-fixture expected minutes and start probability from fixture_player_expected_minutes_resolved_v3: one row per fixture x player x set-piece type. Only players with a set-piece role appear. Input to fpl_set_piece_fixture_adjustments_v1 (live chain). Internal. |  |
| `fpl_team_strength_current` | view | current | no | yes | Premier League team ratings from the latest ACCEPTED Dixon-Coles fit for league_id 1 only: one row per team with attack/defence strength, is_estimated, fit metadata (window, rho, home advantage, half-life, matches used), FPL team id/name, 0-100 percentile attack/defence/overall scores and ranks. Granted to anon and protected by the daily public_views_readable_by_anon guard in check_model_integrity, but no src/ page reads it directly. Model estimates, not observed. |  |
| `league_fit_status` | view | current | yes | yes | Model-fit status per league (competition_type 'league'): one row per league with the latest attempted Dixon-Coles fit (status pending/accepted/rejected, converged, matches used, rejection reason, validation warnings) alongside the current accepted production fit (id, date, matches used, rho, home advantage). Read by the public fit-freshness banner and the /data-health page; scripts/fit_dixon_coles.py also references it. Metadata about the prediction model, not predictions. |  |
| `league_standings` | view | current | yes | yes | Computed league tables from results for every domestic league and season: one row per league x season x team with position, pyramid_position, P/W/D/L, goals, clean sheets, points (after point_deductions), PPG and full home/away splits. Curtailed past seasons are ranked on points per game; is_final is false for the current season. Caveats: the 'tier' column is just league_id (correct for English divisions 1-5, wrong for other countries, e.g. La Liga shows 13); pyramid_position assumes lower league_id = higher division within a country; tie-breaks are GD, GF, then name (not official rules). Read by team history panels. |  |
| `match_predictions` | table | current | yes | yes | Point-in-time Dixon-Coles goal predictions for archived matches (past seasons live in matches, not fixtures): one row per match_id with the fit used (newest accepted fit dated strictly before the match that rates both teams), its as-of date and predicted home/away goals. Used by the model scorecard and betting backtests for prior seasons. Caveat: many fits behind these are retro-fits (model_fit_runs.is_retrofit) made later with point-in-time data, not predictions published at the time. | Written by backfill_match_predictions(league, season), called from scripts/retrofit_verify_and_predict.py in GitHub workflow retrofit-season.yml (manual dispatch). |
| `model_change_log` | table | current | yes | yes | Human-written log of changes to how the model fits, predicts, calculates returns or handles data: one row per change with date, area, title, reason, detail, before/after metrics and version from/to. Shown on the Model Versions & Changes page. | Manual: new entries added by migration in the same PR as the change (scripts/fit_dixon_coles.py instructs this); never written from the site. |
| `model_experiment_predictions` | table | experimental | no | no | Forecasts from offline, point-in-time model experiments (e.g. different half-life/shrinkage settings): one row per experiment x variant x match with the as-of date, predicted goals, rho and H/D/A probabilities. Experiment fits are made in memory and never become live fits. Scored by score_model_experiment(); not shown on the site. | Upserted by scripts/model_experiment.py via GitHub workflow model-experiment.yml (manual dispatch only). |
| `model_fit_runs` | table | current | yes | yes | One row per Dixon-Coles model fit for a league (window, rho, home advantage, decay half-life, log-likelihood, status accepted/rejected with validation checks). Parent of team_ratings; readers must use status='accepted' — the newest row may be rejected. Daily fits cover E0, E1, E2, E3 and EC only. | Written by scripts/fit_dixon_coles.py and estimate_promoted_team_ratings.py in the Daily Premier League Import workflow (06:00 UTC); retro fits via manual Retro-fit past seasons workflow. |
| `model_scorecard_matches` | matview | current | yes | yes | Materialized one-row-per-match evaluation set for the Model Scorecard: every played match (leagues 1-5 via the current-season rule) with a point-in-time prediction (match_predictions for past seasons, fixtures for the current season) and closing market-average 1X2 odds, giving model H/D/A probabilities (Dixon-Coles with the fit's rho), de-margined market probabilities, result, team_type and season phase. Only as fresh as its last refresh; anon SELECT is intentional (check_model_integrity needs it). | Refreshed by refresh_model_scorecard() via pg_cron 'refresh-model-scorecard' at 10:00 and 22:00 UTC. |
| `model_versions` | table | current | yes | yes | Named model settings behind every fit and prediction: one row per version (e.g. legacy_pre_v1, dc_v1, dc_v1_1 for fits; dc_baseline_v1, dc_home_away_sparse_shrink_v2 for predictions) with component, description, params JSON and introduced/retired dates. Referenced by model_fit_runs.model_version and shown on the Model Versions & Changes page. | Manual: rows added by migration when a new version is introduced. |
| `prediction_corrections` | table | current | no | no | Audit trail of fixture predictions corrected after the fact (e.g. those from retired fit #2 or from fits dated on/after kick-off): one row per corrected fixture with reason and old vs new fit, model version and predicted goals. | Written by one-off data-correction SQL (23 Sep 2026, after retro-fits; see docs/incidents.md); no scheduled writer. |
| `tactical_formation_slot_priors` | view | current | yes | yes | Historical attacking output by formation slot from external tactical data sources: one row per tactical source x source formation code x formation slot x venue scope with starts, minutes, goals/assists/key passes/shots/big chances/box touches, per-90 rates, the slot's share of team goals/assists, and the mapped canonical formation. Read by the public /fpl/formations page. Historical reference data (source seasons/competitions vary; see source_season_label), used as priors, not current-season stats. |  |
| `team_finishing_position_projection` | table | current | yes | yes | Monte Carlo projection of final league position: one row per league + season + team (mean projected points/position, median position, current points/played, full position distribution, 20,000 sims). Shown on the Team Strength page. Model estimate from independent Poisson draws of predicted goals, not observed. | scripts/simulate_final_table.py — FPL Projections Pipeline workflow (06:41 and 18:41 UTC) and manual Simulate Final Table workflow. |
| `team_home_away_adjustment_experimental_v1` | view | current | no | no | Despite 'experimental' in the name, this is the live base of team-specific home/away strength deviations: one row per Premier League team (league_id 1 only, matches in the last 730 days, 180-day half-life weighting) with weighted goals for/against overall, home and away, and log home/away attack and defence deviations shrunk by 12 matches. Read only through team_home_away_adjustment_v1, which feeds backfill_fixture_predictions. Internal model input; private (not anon-readable). |  |
| `team_home_away_adjustment_v1` | view | current | no | no | League-centred team home/away attack and defence adjustments: one row per Premier League team, taking team_home_away_adjustment_experimental_v1 and subtracting the weighted league mean deviation (centered_* columns). Joined by backfill_fixture_predictions to adjust Dixon-Coles fixture predictions (run by the Daily Premier League Import workflow and the admin team-ratings page). Deliberately SECURITY DEFINER as the public face of the private experimental view. Only E0 teams have rows; other teams get no adjustment. Internal model weighting. |  |
| `team_ratings` | table | current | yes | yes | Dixon-Coles attack and defence strengths: one row per fit run + team. Only meaningful joined to model_fit_runs with status='accepted'; is_estimated rows are promoted/relegated teams estimated from an adjacent division, not fitted. Feeds predictions, team_strength_current and the Team Strength/team pages. | Written by scripts/fit_dixon_coles.py and estimate_promoted_team_ratings.py in the Daily Premier League Import workflow (06:00 UTC, E0–E3, EC). |
| `team_season_movement` | view | current | yes | yes | For English divisions only (league_id 1-5): one row per league x season x team with the previous season's league and movement ('promoted', 'relegated', 'stayed', 'new', or 'unknown' for the first season on file) plus is_promoted. Used to flag promoted/relegated teams in the model scorecard and betting backtests. Caveat: 'new' (arrived from outside the data, e.g. from below the National League) counts as promoted; non-English leagues are not covered. |  |
| `team_strength_current` | view | current | no | yes | Team ratings from the latest ACCEPTED Dixon-Coles fit per league: one row per league x team with attack/defence strength, is_estimated (e.g. promoted teams), fit metadata (window, rho, home advantage, half-life, matches used, fitted_at), 0-100 percentile attack/defence/overall scores, ranks within league, and FPL team mapping where available. Granted to anon and protected by the daily public_views_readable_by_anon guard, but no src/ page reads it directly (pages query team_ratings/model_fit_runs). Model estimates, not observed. |  |
| `team_strength_manual_override` | table | current | yes | yes | Admin manual adjustment to a team's attack/defence strength (one row per team, with note). Applied by backfill_fixture_predictions() to published predictions and shown on Team Strength and team pages. Human judgement layered on the model — compare raw_predicted_* on fixtures for the unadjusted model. | Admin edits on /admin/team-ratings. |
| `fixture_derived_markets(p_lambda_home numeric, p_lambda_away numeric, p_rho numeric)` | function | current | no | yes | Pure Dixon-Coles maths: given home/away expected goals (lambdas) and rho, builds the 0-8 x 0-8 scoreline grid with the low-score correction and returns percentages for home win, draw, away win, over/under 2.5, BTTS and each side's clean sheet. IMMUTABLE; model output only as good as its inputs. | Called inside get_model_accuracy, get_betting_bets and view model_scorecard_matches whenever those are queried. |
| `get_fpl_fixture_bonus_v4(p_fixture_id bigint)` | function | current | no | no | Model estimate of expected FPL bonus points per player for one fixture, using a closed-form pairwise comparison of projected BPS (from fpl_fixture_bps_projection_v1) to approximate P(rank 1/2/3). Model output, not observed bonus; expensive (triple self-join). Now only the fallback used when a fixture has no Monte Carlo bonus (fpl_fixture_bonus_montecarlo_v1). | Called inside refresh_fpl_projection_fixture_v6_impl() for fixtures lacking Monte Carlo coverage; that runs from scripts/refresh_fpl_projections.py in the fpl-projections-pipeline workflow (06:41 and 18:41 UTC) and manual refresh-fpl-projections workflow. |
| `score_model_experiment(p_experiment text)` | function | experimental | no | no | Scores an offline model experiment: for each variant x league x season, log-loss of the variant's H/D/A probabilities vs de-margined closing market-average odds, plus predicted vs actual draws and goals, computed only on matches every variant predicted and only where as_of_date is before the match (point-in-time guard). | Called by scripts/model_experiment.py after writing model_experiment_predictions (GitHub workflow model-experiment.yml, manual dispatch only). |

## fantasy

FPL data and FixtureShark's FPL projections.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `fixture_actual_lineup_players` | table | current | yes | yes | Observed starting line-ups and minutes per fixture: one row per fixture + team + player (starter flag, minutes, FPL player id, tactical role, source). Used via the view fpl_prediction_actual_start_comparison to compare predicted vs actual starters on FPL pages. Observed data, but coverage is whatever has been captured — not every fixture. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `fixture_lineup_prediction_players` | table | current | no | no | Players named in each third-party predicted line-up: one row per lineup_prediction + FPL player (predicted_start, source start probability, tactical role). Rolled up by fixture_player_lineup_consensus / fixture_player_tactical_consensus into weighted consensus start probabilities and roles for the FPL projection chain. Source predictions, not observed line-ups. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `fixture_lineup_predictions` | table | current | no | no | Header rows for third-party predicted line-ups: one row per fixture + team + lineup source observation (predicted formation, observed_at). Parent of fixture_lineup_prediction_players; consumed by the consensus views (fixture_team_tactical_consensus etc.) feeding FPL projections and the Starting Lineups page. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `fixture_player_expected_minutes_resolved_v3_raw` | view | current | no | no | Intermediate FPL minutes model: one row per fixture x team x FPL player with expected minutes, start/sub/availability probabilities and minutes_source, before team-sheet limits are applied (overlays squad-state overrides and v6 fallback start probabilities on fixture_player_expected_minutes_resolved). Caveat: raw totals overstate minutes (~1.37 starting GKs per team); use fixture_player_expected_minutes_resolved_v3, which caps it to 1 GK / 10 outfield starters. |  |
| `formation_code_names` | table | current | yes | no | Lookup mapping Opta numeric formation codes to readable formation names (e.g. code -> '4-4-2'); one row per source formation code. Used by the FPL Formations page. | Static reference data (one-off load from the Opta formation guide). |
| `formation_slot_geometry` | table | current | yes | no | Pitch coordinates for each slot in each Opta formation: one row per formation code + slot (x_pct, y_pct). Used to draw players in the right place on formation pitch graphics (Formations page, tactical roles UI). | Static reference data (one-off load from the Opta formation guide). |
| `fpl_fixture_bonus_montecarlo_v1` | table | current | no | no | Monte Carlo estimate of FPL bonus points: one row per (fixture_id, fpl_player_id), expected_bonus_points averaged over simulated BPS rankings (Poisson goals/assists, correlated team clean sheets). It is an intermediate input: refresh_fpl_projection_fixture_v6_impl copies it into fpl_player_projections.expected_bonus/xpts_bonus when present, otherwise falls back to the closed-form get_fpl_fixture_bonus_v4. Use fpl_player_projections for bonus figures, not this table; fixture_id is the FixtureShark fixtures id, not the FPL fixture id. | Written by scripts/simulate_fixture_bonus.py (upsert on fixture_id,fpl_player_id) in the 'FPL Projections Pipeline' GitHub workflow (cron 41 6 * * * and 41 18 * * * UTC, between the two projection-refresh passes) for the next ~3 matchweeks; also the manual 'Simulate Fixture Bonus' workflow. Not written by refresh_fpl_bonus_v3_for_fixture (the old catalogue note was wrong). |
| `fpl_fixtures` | table | current | yes | yes | FPL's own fixture list as returned by the official FPL API: one row per (fpl_fixture_id, season_id), with FPL event (gameweek), kickoff, FPL team ids, scores/finished flags and FPL's difficulty ratings (team_h/a_difficulty). canonical_fixture_id links to FixtureShark's fixtures table. Used by Fantasy actual-match, player and season pages. FPL reuses fpl_fixture_id every season, so always filter or join on season_id; prefer fixtures/matches for canonical dates and results. | Upserted by private.refresh_fpl() via pg_cron job fpl-refresh-6-hourly (17 */6 * * *). |
| `fpl_gameweeks` | table | current | yes | yes | FPL gameweeks (events): one row per (fpl_event_id, season_id) with name, transfer deadline_time, finished/data_checked, is_previous/is_current/is_next flags and FPL-wide average and highest scores. Read by snapshot/Model XI functions (get_model_xi_history is called by the public Model XI page) and fpl_current_season_id/fpl_gameweek_for_date. fpl_event_id repeats each season, so filter on season_id. | Upserted by private.refresh_fpl() via pg_cron job fpl-refresh-6-hourly (17 */6 * * *). |
| `fpl_hindsight_optimal_squad` | table | current | yes | yes | Results of the exact hindsight solver: one row per (season_id, league_id, from_matchweek, to_matchweek, budget, optimiser_version) holding the best possible FPL squad/XI/captain using REAL points already scored, in the result JSON (squad, weekly_plan, notes) plus objective_points and solver status. The public page /fpl/optimal-squad-so-far shows the latest computed_at row. It is hindsight, not a projection or recommendation; bench points and auto-subs are not modelled. | Upserted by scripts/solve-hindsight-optimal.ts in the 'Hindsight Optimal Squad' GitHub workflow (cron 45 6 * * * UTC, plus manual dispatch). |
| `fpl_ingestion_runs` | table | current | yes | no | Run log for the raw FPL API sync: one row per private.refresh_fpl() run with start/finish, status and counts of teams, players, gameweeks, fixtures and player-gameweeks upserted. Shown on the Data Health page and checked by check_model_integrity (stuck 'running' rows). Caveat (OUTSTANDING.md): a run that fails and re-raises can roll back its own status write, so a failed run may leave no row; absence of a failure row is not proof of success. | Written by private.refresh_fpl() on each pg_cron fpl-refresh-6-hourly run (17 */6 * * *). |
| `fpl_player_defensive_contribution_usage` | view | current | no | yes | Season-to-date defensive actions per FPL player: one row per season x player with appearances, minutes and summed clearances/blocks/interceptions (CBI), recoveries and tackles, parsed from fpl_player_gameweeks.source_payload stats. Observed FPL data aggregated; input to fpl_defensive_contribution_projection_leaguewide, fpl_fixture_bps_projection_v1 and scripts/simulate_fixture_bonus.py. |  |
| `fpl_player_gameweek_history` | table | current | yes | yes | Historic FPL per-gameweek player scores for PAST seasons (2021/22 to 2025/26 per OUTSTANDING.md): one row per (season_id, fpl_code, gameweek, fixture_id) with points, minutes, goals, assists, bonus/BPS, price (value, tenths of £m), ownership and transfers. Keyed on fpl_code (stable player id across seasons via player_identity), not fpl_player_id; fixture_id and opponent_team_num are FPL's ids for that season, not FixtureShark fixtures. Current-season gameweek data lives in fpl_player_gameweeks instead. Powers player career/season pages, Team of the Week and Season XI weekly views via RPCs. | One-off manual imports per past season via import_fpl_gameweeks(season_id, folder) from the vaastav/Fantasy-Premier-League GitHub CSVs (service_role only); not scheduled. Row estimate 0 in stats is wrong (OUTSTANDING.md: ~99k rows). |
| `fpl_player_gameweeks` | table | current | yes | yes | Current-season FPL per-match player stats from the official FPL API: one row per (fpl_player_id, fpl_fixture_id, season_id) with points, minutes, goals, assists, clean sheets, bonus/BPS, ICT, xG/xA, price and transfer counts. A double gameweek gives two rows for one fpl_event_id. Used across the Fantasy pages (actual matches, player pages, Team of the Week, value tables), the bonus simulation's BPS priors and sitemap/static generation. fpl_player_id is only unique within a season; use fpl_code/player_identity across seasons. | Upserted by private.refresh_fpl() (pg_cron fpl-refresh-6-hourly, 17 */6 * * *) and by the live-event refresh private.refresh_fpl_current_event() (pg_cron fpl-live-current-6-hourly, 23 */6 * * *); a BEFORE INSERT trigger fills season_id. The live-event path only writes players whose team has exactly one fixture in that event. |
| `fpl_player_projections` | table | current | yes | yes | FixtureShark's FPL expected-points model output: one row per (fixture_id, fpl_player_id, model_version) with expected minutes, start/sub/availability probabilities, xG/xA, clean-sheet and defensive-contribution probabilities, expected bonus and the xpts_* components summing to expected_fpl_points. Read by the Fantasy pages, optimiser RPCs, landing page picks and static generation. Contains more than one model_version (older leaguewide_v4 rows remain): filter model_version = 'leaguewide_v6' (CURRENT_MODEL_VERSION in src/lib/fplApi.ts) and scenario_key = 'baseline'. These are model estimates, not FPL data; fixture_id is the FixtureShark fixtures id. | Written by refresh_fpl_projection_fixture_v6 per fixture, called by scripts/refresh_fpl_projections.py in the 'FPL Projections Pipeline' GitHub workflow (41 6 * * * and 41 18 * * * UTC, two passes around the bonus simulation); also manual 'Refresh FPL Projections' and 'Backfill Projections' workflows. Played fixtures are not overwritten by the routine pipeline. |
| `fpl_player_return_assumptions` | table | unused | no | no | Intended store for manual injury-return assumptions per player (expected return date, uncertainty, pre/post-return availability and start probability, source, is_active). Nothing in the repo reads or writes it: no view, function, script or page references it beyond the generated TypeScript types, and it has no anon read policy. Do not assume its contents affect projections. | None found: no writer in src/, scripts/, workflows, edge functions or definitions. |
| `fpl_player_season_totals` | table | current | yes | yes | Historic FPL season totals for PAST seasons: one row per (season_id, fpl_code) with web/full name, position (element_type 1 GK to 4 FWD), start_cost and end_cost (tenths of £m), and season points, minutes, goals, assists, bonus etc. Only players with minutes > 0 were imported. Used by Season XI (direct read), player scout/search, player career and seasons RPCs; it also maps each season's element id (fpl_element_id) to fpl_code for import_fpl_gameweeks. Current-season totals are in fpl_players. | One-off manual imports per past season via import_fpl_season(season_id, folder) from vaastav/Fantasy-Premier-League players_raw.csv (service_role only); not scheduled. Row estimate 0 in stats is unreliable. |
| `fpl_player_snapshots` | table | current | yes | yes | Daily history of FPL player market state: one row per (snapshot_date, fpl_player_id) with price (now_cost, tenths of £m), ownership %, total/event points, status, chance of playing, news and transfer counts. Later refreshes on the same day overwrite that day's row. Powers price-change risk, market movers, injury report and daily/gameweek digest RPCs on public pages. Only covers the current season from when capture began. | Written by private.refresh_fpl() via pg_cron fpl-refresh-6-hourly (17 */6 * * *); BEFORE INSERT trigger fills season_id. |
| `fpl_player_squad_state` | table | current | no | no | Manual and feed-sourced squad-availability overrides for the FPL projection model: one row per (season_id, fpl_player_id, effective_from) with state (active/doubtful/injured/suspended), availability probability, optional start-probability override and evidence/source. The view fpl_player_squad_state_current picks the live row, which feeds expected-minutes resolution and so projections. Internal model input, not a public injury list (use fpl_players status/news or get_injury_report). | Written by admins from the Tactical Roles admin page (src/lib/tacticalRoleAdminApi.ts, source_name 'manual_tactical_override', upsert/delete); RLS allows authenticated admin writes. Note the page hard-codes season_id 13. |
| `fpl_player_squad_state_current` | view | current | no | yes | Currently effective squad-state override per player (e.g. injured, suspended, manual tactical override): one row per season x FPL player, picking the active fpl_player_squad_state row (manual_tactical_override first, then most recent effective_from) with availability_probability, start_probability_override, source and evidence. Overrides start chance/availability in fixture_player_expected_minutes_resolved_v3_raw (minutes_source 'squad_state_override'); rows are written from the tactical-roles admin page and other squad-state sources. Current-state only; history lives in fpl_player_squad_state. |  |
| `fpl_player_substitution_usage` | view | current | no | yes | Season-to-date appearance pattern per FPL player: one row per season x player with appearances (minutes > 0), likely_starts (60+ minutes, a proxy, not actual starts), sub_appearances (1-59 minutes) and average minutes in those short appearances (default 20). Derived from fpl_player_gameweeks; input to the fallback start/sub probabilities (fixture_player_expected_minutes_fallback, _v2, fpl_fallback_start_probability_v6). |  |
| `fpl_players` | table | current | yes | yes | Current FPL player records from the official FPL API: one row per (fpl_player_id, season_id) with names, slug, position (element_type 1 GK, 2 DEF, 3 MID, 4 FWD), FPL team, price (now_cost, tenths of £m), ownership, status/news/chance of playing and season-to-date stats (points, minutes, goals, xG, ICT etc.). Core table for every Fantasy page and projection. fpl_player_id is reused by FPL each season; fpl_code (and player_identity) is the stable cross-season id. Stats are as of the last refresh, not per match. | Upserted by private.refresh_fpl() via pg_cron fpl-refresh-6-hourly (17 */6 * * *). Triggers fill season_id, fpl_code and slug and sync player_identity (sync_player_identity). |
| `fpl_projection_fixture_snapshots` | table | current | no | yes | Append-only record of each fixture's predicted home/away goals and kick-off as they stood when an FPL gameweek's projections were snapshotted: one row per season x gameweek x snapshot_kind x fixture. Pairs with fpl_projection_snapshots to score projections honestly against pre-deadline predictions. UPDATE/DELETE are refused by trigger. | Inserted by snapshot_fpl_projections(), driven by pg_cron 'snapshot-fpl-projections' every 10 minutes (pre_deadline/late), plus occasional manual snapshots. |
| `fpl_projection_model_parameters` | table | unused | no | no | Intended table of FPL projection model parameters (season, model_version, parameter_name, optional position, value, notes). Nothing in the repo reads or writes it (only the generated TypeScript types mention it), and it has no anon read policy; the live projection views hard-code their constants instead. | None found: no writer in src/, scripts/, workflows, edge functions or definitions. |
| `fpl_projection_snapshots` | table | current | no | yes | Append-only record of what the FPL projection model said before each gameweek deadline: one row per season x gameweek x snapshot_kind ('pre_deadline', 'late', 'manual') x fixture x player x model_version, with expected minutes, goals, assists, clean-sheet probability, points components and expected_fpl_points. The only honest pre-match record (fpl_player_projections is overwritten and many stored projections were back-filled after kick-off). Caveats: started 26 Sep 2026 (GW6 onwards); filter by model_version and snapshot_kind to avoid double counting. | Inserted by snapshot_fpl_projections(), driven by pg_cron 'snapshot-fpl-projections' every 10 minutes; UPDATE/DELETE refused by trigger. |
| `fpl_scoring_rules` | table | current | yes | yes | Reference list of official FPL scoring rules: one row per rule_code (optionally per position) with points, threshold and notes, per season. Shown on the public /fpl/scoring-rules page and used for trivia. The page does not filter by season_id, so rows for more than one season would be shown together. | Static reference data; no seeding script or migration found in the repo (loaded manually). |
| `fpl_teams` | table | current | yes | yes | FPL's own team list: one row per (fpl_team_id, season_id) with name, short_name, FPL code and FPL's strength ratings (overall/attack/defence, home/away). canonical_team_id maps to FixtureShark's teams. The strength numbers are FPL's published ratings, not FixtureShark's Dixon-Coles ratings. Read indirectly by public views (fpl_team_strength_current, team_strength_current) and FPL RPCs; fpl_team_id repeats each season. | Upserted by private.refresh_fpl() via pg_cron fpl-refresh-6-hourly (17 */6 * * *). |
| `lineup_prediction_sources` | table | current | no | no | Registry of predicted-line-up sources with a weight and active flag; one row per source. Weights drive the consensus views over fixture_lineup_predictions — internal weighting, not public. | Manual configuration (no writer in repo). |
| `player_identity` | table | current | yes | yes | Cross-season identity for FPL players: one row per FPL 'code' (stable across seasons) with canonical name, first/last season seen and URL slug. Links a person's fpl_players rows across seasons for player pages, career records, player scout and search. | Trigger: sync_player_identity() on fpl_players insert/update; also import_fpl_season(); slug set by player_identity_slug trigger. |
| `player_squad_hierarchy` | table | current | yes | no | Squad pecking order per season, team and FPL player (squad_status, hierarchy_score, evidence). Feeds the expected-minutes fallback views (fixture_player_expected_minutes_fallback, fpl_fallback_start_probability_v6) and FPL fixture/squad pages. Curated judgement, not observed data. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `player_tactical_profiles` | table | current | no | no | Season-level tactical profile per team + player from an external source (role, role confidence, start/appearance/60+ probabilities, expected minutes if start/sub). Read only by fixture_player_expected_minutes_fallback (E0 current season) as a fallback prior. Model/source estimates, not observed. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `season_best_xi` | table | current | yes | yes | Stored hindsight-optimal 'Set-and-Forget' FPL XI for completed seasons: one row per season + player (fpl_code, position, start cost, total points). Powers /fpl/season-xi. Historic solved answer, not a prediction. | Manual: solved once per completed season and inserted by hand (OUTSTANDING.md). |
| `set_piece_hierarchies` | table | current | yes | yes | Set-piece taker order per season + team + set-piece type (penalties, direct free kicks, corners): one row per ranked player with confidence and validity dates. Feeds get_set_piece_takers() (player pages), get_set_piece_index(), fpl_set_piece_fixture_exposure_v1 and the Starting Lineups/Tactical Roles pages. Curated, not observed. | Admin edits on the Tactical Roles page (/fpl/tactical-roles); initial load not in repo. |
| `tactical_role_priors` | table | current | no | no | Per tactical role goal and assist weights (with role group and evidence basis); one row per role. Used by fpl_projection_leaguewide_allocation_v2 to share a team's expected goals/assists among players. Internal model weighting. | Static/manual model parameters (no writer in repo). |
| `team_player_tactical_defaults` | table | current | yes | no | Default tactical role, depth rank and manual availability status per season + team + FPL player (default source Fantasy Football Scout, corrected by admin). Fallback role source for fixture_player_tactical_consensus and refresh_fpl_projection_fixture_v6; shown on Starting Lineups. Curated, not observed. | Admin edits on the Tactical Roles page (/fpl/tactical-roles); initial seed not in repo. |
| `team_tactical_defaults` | table | current | yes | no | Default formation per season + team with confidence and source ('manual' overrides beat line-up-source consensus). Read by fixture_team_tactical_consensus and the Starting Lineups/Tactical Roles pages. | Admin edits on the Tactical Roles page; non-manual seed not in repo. |

## finance

Club accounts from Companies House (owned by the finance ingestion workstream).

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `finance_derived_metrics` | view | current | yes | yes | FixtureShark-derived club finance ratios, one row per club (team_id) x reporting period_end x metric_key; currently only staff_cost_ratio (staff costs / total revenue), with its definition text and calculation_version. Computed by FixtureShark, not a filed figure. Read by the club finance pages and build-time static finance pages. Security-invoker view: anon/authenticated see only published, current-version filing data via RLS on finance_periods/finance_metric_values; a service-role reader would also see unpublished/draft periods. |  |
| `finance_filings` | table | current | no | no | One row per statutory accounts filing (Companies House) for a reporting entity: filing/document ids, source URL, filing date, period start/end and months, consolidation, and extraction_status (discovered, extracted, mapped, validated) and validation_status. Private raw layer behind the finance pages; not readable by anon. Current production slice is Southend United FY2019-FY2025. | Owned by the separate finance team; loaded ad hoc (not scheduled) from Companies House iXBRL filings using scripts/import_finance_ixbrl.py and their own loading/review steps (docs/finance-ingestion.md). |
| `finance_ingestion_runs` | table | current | no | no | Run log for finance ingestion: one row per ingestion run with source, timings, status, counts of filings discovered/processed, facts extracted/mapped and errors. Operational only; private. | Owned by the separate finance team; loaded ad hoc (not scheduled) from Companies House iXBRL filings using scripts/import_finance_ixbrl.py and their own loading/review steps (docs/finance-ingestion.md). |
| `finance_metric_dictionary` | table | current | yes | yes | Definitions of the published finance metrics: one row per metric_key with display name, plain-English definition, metric/statement type, value kind and expected sign (18 rows per docs/finance-ingestion.md). Publicly readable; used by the finance pages (financeApi getFinanceDictionary and the static build) to label and explain figures. | Static reference data, seeded by the finance baseline migration 20260921101758_finance_schema_baseline.sql; changed only by the finance team. |
| `finance_metric_mappings` | table | current | no | no | Versioned rules mapping XBRL concepts to FixtureShark finance metric_keys: one row per (source, xbrl_concept, metric_key, mapping_version) with context rules, notes and is_active. Private; used during the finance team's normalisation step to cope with taxonomy differences between filing years. | Owned by the separate finance team; loaded ad hoc (not scheduled) from Companies House iXBRL filings using scripts/import_finance_ixbrl.py and their own loading/review steps (docs/finance-ingestion.md). |
| `finance_metric_values` | table | current | yes | no | Normalised finance figures: one row per (financial_id, metric_key) with value, currency, unit_scale and full lineage back to the raw fact, XBRL concept, filing and mapping version, plus validation_status. RLS lets anon see only rows that are published and belong to a published, current-version period; the public finance pages read them through the finance_published_* and finance_derived_metrics views. The table itself also holds unreviewed rows, so an assistant should use the published views. Missing disclosures are NULL, never zero. | Owned by the separate finance team; loaded ad hoc (not scheduled) from Companies House iXBRL filings using scripts/import_finance_ixbrl.py and their own loading/review steps (docs/finance-ingestion.md). |
| `finance_periods` | table | current | yes | no | One row per club reporting period per filing version: team_id, filing, period dates/months, company, consolidation, currency/unit scale, season mapping (and its confidence), is_latest/is_current_version/is_comparable and validation_status/published_at. RLS exposes only published current-version rows to anon; the public finance pages read them via finance_published_periods. Caveats: financial years do not align with football seasons (season mapping has a confidence level); FY2024 Southend is flagged is_comparable = false. | Owned by the separate finance team; loaded ad hoc (not scheduled) from Companies House iXBRL filings using scripts/import_finance_ixbrl.py and their own loading/review steps (docs/finance-ingestion.md). |
| `finance_published_periods` | view | current | yes | yes | Club statutory accounts in wide form: one row per club (team_id) per reporting period (financial_id), with revenue split (total/matchday/broadcast/commercial/other), staff costs, player amortisation/impairment, profit lines, cash, borrowings, assets/liabilities, average employees, plus period metadata (currency, unit_scale, is_consolidated, is_latest, is_comparable, filing_date, source_url, validation_status). Source is Companies House iXBRL filings (see docs/finance-ingestion.md); missing disclosures are NULL, never zero, and values must be read with currency and unit_scale. Powers /finance, /finance/compare and /football/teams/:slug/finances. Only published, current-version rows are visible to anon via RLS; the view itself has no status filter, so a service-role reader also sees unpublished/draft periods. |  |
| `finance_published_provenance` | view | current | yes | yes | Where each club finance figure came from: one row per club x period_end x metric_key, with the original XBRL concept, original value and unit, filing reference, document id, source URL and mapping version. Used on the club finance page (and build-time static pages) to cite sources. RLS limits anon to published, current-version values; a service-role reader would also see unpublished mappings. |  |
| `finance_raw_facts` | table | current | no | no | Every inline-XBRL fact extracted from a filing: one row per fact with concept, context, period/instant, original and numeric value, unit, scale, dimensions and source locator. Private raw layer; not for public or assistant use (facts are unreviewed and unmapped). | Owned by the separate finance team; loaded ad hoc (not scheduled) from Companies House iXBRL filings using scripts/import_finance_ixbrl.py and their own loading/review steps (docs/finance-ingestion.md). |
| `finance_reporting_entities` | table | current | no | no | Links a FixtureShark team to the legal company whose accounts are used: one row per (team_id, company_number) with entity name, relationship type, preferred flag and effective dates. Private. | Owned by the separate finance team; loaded ad hoc (not scheduled) from Companies House iXBRL filings using scripts/import_finance_ixbrl.py and their own loading/review steps (docs/finance-ingestion.md). |

## broadcast

UK TV/streaming listings for the Watch Guide.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `affiliate_partners` | table | current | yes | no | Admin-maintained affiliate-link config (one row per partner destination domain x market; categories streaming/tickets/merchandise/travel/stadium_experiences). The site's resolver (src/lib/commercialLinks.ts) rewrites a watch_url to the partner's affiliate_url_template only when the row is active and in its valid date window. Caveat: seeded with 4 inactive placeholder rows (NOW, Sky, DAZN, Amazon) with no templates, so currently every link passes through unchanged. | Manual: admin/SQL edits only (no admin UI yet); seeded by migration 20260924150000_affiliate_partners.sql. |
| `broadcast_competition_map` | table | current | no | no | Lookup from Airtable competition labels (e.g. 'Carabao Cup', 'Champions League') to FixtureShark league codes (LC, UCL...). One row per label. Used by the broadcast sync to restrict fixture matching to the right competition; unmapped labels make a record unmatched. | Static reference data seeded by migration 20260925190000_watch_guide_schema.sql; new labels added by admin insert. |
| `broadcast_sync_runs` | table | current | no | no | Run log of the Airtable broadcast sync: one row per sync_airtable_broadcasts call with records seen, inserted, updated, unchanged, unmatched counts and API notes (pages, Airtable ticks, pruned records). kept_stronger is always 0 since 25 Sep 2026 (that branch was removed). Admin-only (RLS). | Inserted by sync_airtable_broadcasts(); notes updated by sync_airtable_broadcasts_from_api() (pg_cron 'sync-airtable-broadcasts-3-hourly', minute 40 every 3 hours). |
| `broadcast_sync_unmatched` | table | current | no | no | Airtable broadcast records the sync could not attach to a fixture (unknown team, unmapped competition, no/ambiguous fixture within 3 days, missing fields): one row per Airtable record id with the reason and full raw record. resolved_at is set automatically when a later sync matches it; open rows are the to-do list for fixing aliases or fixtures. Admin-only (RLS). | Upserted by sync_airtable_broadcasts() on every sync (pg_cron every 3 hours). |
| `fixture_broadcasts` | table | current | yes | yes | UK (market 'GB') TV/streaming information per fixture: zero or more rows per fixture. status 'confirmed_broadcast' = one viewing offer (broadcaster, channel/service, access type free/subscription/PPV, delivery methods, confidence, watch_url); status 'confirmed_not_televised' = explicit evidence it is not shown. Caveat: NO row means 'not yet determined', never 'not on TV'; confidence can be 'probable' or 'needs_verification'; uk_available=false offers are hidden from the guide. Read by match pages, the gameweek browser and the TV guide. | pg_cron 'sync-airtable-broadcasts-3-hourly' (sync_airtable_broadcasts_from_api, minute 40 every 3 hours) from the Airtable UK Broadcasts base; rows are never deleted by the sync. Admins can also write directly (RLS is_admin()). |
| `upcoming_broadcast_fixtures` | view | superseded | no | no | Older broadcast feed: one row per confirmed_broadcast offer for fixtures from today onwards, with broadcaster/channel/access flags, fixture, league, teams and predicted goals. Superseded by upcoming_watch_guide (richer columns, not-televised rows, uk_available filter); its only reader, getUpcomingBroadcastFixtures in src/lib/broadcastsApi.ts, has no callers. Not security_invoker. |  |
| `upcoming_watch_guide` | view | current | yes | yes | UK TV guide feed: one row per viewing offer (or explicit not-televised row) for fixtures from today onwards, with broadcaster, service, access type, delivery methods, confidence, source and watch_url plus fixture, league, teams, team countries and predicted goals. Excludes offers with uk_available=false. Fixtures with no rows are 'unknown', not 'not on TV'. Powers the TV Guide page. |  |
| `broadcast_confidence_rank(p text)` | function | unused | no | no | Immutable helper that ranks a TV-broadcast confidence label (confirmed_primary 4, confirmed_secondary 3, probable 2, needs_verification 1, anything else 3). It was used by sync_airtable_broadcasts to keep the stronger of two confidence labels. | No current caller: migration 20260925230000_airtable_auto_sync.sql removed the only branch that called it from sync_airtable_broadcasts, and the exported live sync_airtable_broadcasts no longer references it. |
| `resolve_broadcast_team(p_raw text)` | function | current | no | no | Maps a free-text team name from the Airtable UK Broadcasts base to a team_id: tries an 'Airtable' alias, then canonical/display name (case-insensitive), then any other source's alias; a level counts only if it identifies exactly one club, otherwise NULL (ambiguous names are reported, never guessed). | Called by sync_airtable_broadcasts() for each record's home and away team. |
| `sync_airtable_broadcasts(p_records jsonb)` | function | current | no | no | Core Airtable -> fixture_broadcasts upsert: for each Airtable record resolves both teams and competition (broadcast_competition_map), finds the nearest fixture within 3 days, and inserts/updates one fixture_broadcasts row per record (keyed on airtable_record_id; adopts pre-sync hand-entered rows). Records it cannot place go to broadcast_sync_unmatched with a reason; each call logs one broadcast_sync_runs row. Idempotent; the latest Airtable version always wins. | Called by sync_airtable_broadcasts_from_api() (pg_cron every 3 hours); was also run manually with connector payloads before the API pull existed. |
| `sync_airtable_broadcasts_from_api(p_update_airtable boolean)` | function | current | no | no | Pulls every record from the Airtable UK Broadcasts table (token from Vault), normalises select fields, and runs sync_airtable_broadcasts on them; if p_update_airtable, ticks/unticks Airtable's 'Synced to site' flag and deletes Airtable records for matches played more than a day ago (free-plan 1,000-record cap). Writes a note on the broadcast_sync_runs row. Site rows in fixture_broadcasts are never deleted by this. | pg_cron 'sync-airtable-broadcasts-3-hourly' at minute 40 every 3 hours (select sync_airtable_broadcasts_from_api(true)). |

## api

Functions and views the site calls to render pages.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `fpl_optimizer_candidate_feed_v2` | view | current | no | yes | FPL squad-optimiser candidate list: one row per current-season fixture x player from fpl_projection_frontend_feed_v6 (leaguewide_v6), with matchweek, team and opponent, home/away, FPL price (now_cost and price_m, current price not historical), expected minutes, start/sub probabilities, expected FPL points and lineup confidence. Read by get_fpl_optimizer_candidates and get_fpl_optimizer_candidates_json; no caller of those RPCs was found in src/, scripts or workflows. Model projections, not observed points. |  |
| `fpl_prediction_actual_start_comparison` | view | current | yes | yes | Predicted vs actual starts/minutes: one row per fixture x player who actually started or played (from fixture_actual_lineup_players), with actual_started/actual_minutes alongside the most recently generated fpl_player_projections row for that fixture/player (any model_version) and a generated_pre_kickoff flag. Powers actual-vs-predicted tables on the public FPL fixture/actual-matches/gameweek pages. Caveats: the prediction may be from an older model version or generated after kickoff (check generated_pre_kickoff); players who were predicted but did not play are absent. |  |
| `fpl_projection_frontend_feed_v6` | view | current | yes | yes | Main public FPL projection feed: one row per fixture x player from fpl_player_projections where model_version = 'leaguewide_v6' (the current production model), with kickoff date, team, FPL position, tactical role, minutes_source, expected minutes, start/sub probabilities (clamped to be consistent), expected goals/assists, clean-sheet and defensive-contribution probabilities, expected bonus, expected FPL points, lineup confidence and generated_at. Read by the public FPL fixture projection page and squad-pitch views, and by fpl_optimizer_candidate_feed_v2. Model projections, not observed results; numbers change on every pipeline run. |  |
| `fpl_season_fixture_feed` | view | current | yes | yes | Current FPL season's Premier League (E0) fixture list: one row per fixture with matchweek, round, kickoff date/time, status, home/away team ids and names, Dixon-Coles predicted home/away goals, predicted_at and a has_projection flag (both team predictions present). Read by the public FPL season/gameweek pages (fixture lists, gameweek navigation, default matchweek). Predicted goals are model estimates. |  |
| `fpl_season_player_projection_feed` | view | current | yes | yes | Season projection feed for the FPL gameweek browser: one row per current-season fixture x player from fpl_player_projections (model_version leaguewide_v6) with matchweek, kickoff date, team, position, tactical role (from fixture_player_tactical_consensus), expected minutes, start/sub probabilities, expected goals/assists, clean-sheet and defensive-contribution probabilities and expected bonus (as experimental_expected_bonus). Excludes expected FPL points (the app merges those from fpl_player_projections); shrunk_xg90/xa90 are always NULL. Read by the public gameweek page. Model projections. |  |
| `get_actual_value_table(p_season_id bigint)` | function | current | yes | yes | FPL value table: every player with points > 0 from the latest FPL snapshot, with price (£m), total points, points per £m, season minutes/goals/assists/clean sheets/bonus (summed from fpl_player_gameweeks for p_season_id, default 13) and ownership %, sorted by points per £m. Observed FPL data, not model output. Used by /fpl/value (ValuePage) and the FPL hub landing tiles. Caveat: uses the latest fpl_player_snapshots date regardless of season, and joins fpl_players on fpl_player_id only (PK is fpl_player_id+season_id), so it will mis-join once fpl_players holds more than one season. | Page load: /fpl/value and landing tiles via src/lib/valueApi.ts. |
| `get_all_time_top_scorers()` | function | current | yes | yes | Top 4 TEAMS (not players) by total goals scored across every match in public.matches (all leagues and seasons loaded), with a count of distinct seasons covered. Despite the name it has nothing to do with individual goalscorers. Used for a trivia tile on the football hub. | Page load: football hub trivia carousel via src/lib/landingApi.ts. |
| `get_best_defence_rating(p_league_id bigint)` | function | unused | no | yes | Top 4 teams by model defensive strength for a league: exp(-(defence_strength + manual defence_adjustment)) from team_ratings of the latest ACCEPTED Dixon-Coles fit, i.e. expected goals conceded per game against an average attack. Model estimate, not observed goals conceded; includes manual overrides. | Not called anywhere in the repo (only in generated types). |
| `get_betting_bets(p_edge numeric, p_market text, p_closing boolean, p_best_price boolean, p_stake numeric, p_season_id bigint, p_league_id bigint, p_promoted text)` | function | current | yes | yes | Backtest bet list for the Model Returns page: one row per simulated bet (match x selection) where the Dixon-Coles model's probability beat the bookmaker price by at least p_edge, with stake, price, result and profit. Uses point-in-time predictions (fixtures for the current season, match_predictions for archived matches) and match_odds; filters by market ('1x2' or 'ou25'), closing vs opening odds, best price vs market 'Avg', league and promoted-team status. Caveats: hypothetical returns, not real bets; p_season_id defaults to 13 (2026/27); 'Max'/'Avg' odds rows are market aggregates, not bookmakers; retrofit=true rows come from later retro-fits, not predictions made at the time. | Called on demand by the site (src/lib/bettingApi.ts -> ModelReturnsPage / BetList) and by get_betting_returns. |
| `get_betting_returns(p_edge numeric, p_market text, p_closing boolean, p_best_price boolean, p_stake numeric, p_season_id bigint, p_league_id bigint, p_promoted text)` | function | current | yes | yes | Summary of get_betting_bets with the same parameters: one row per selection (Home/Draw/Away or Over/Under 2.5) with bets, staked, returned, profit, ROI %, hit rate, average odds and average edge. Built on get_betting_bets so totals always agree with the bet list. Hypothetical backtest returns, not real betting. | Called on demand by the site (src/lib/bettingApi.ts -> /football/model-returns). |
| `get_biggest_comebacks()` | function | unused | no | yes | Up to 8 matches from public.matches (all leagues/seasons) where a team trailing by 2+ goals at half-time went on to win, with HT/FT scores, date, league code and HT deficit. Observed results. | Not called anywhere in the repo (only in generated types). |
| `get_completed_gameweeks(p_season_id bigint)` | function | current | yes | yes | List of completed FPL gameweeks for a season (default 13) with player count, total points and best individual score per gameweek. For historic seasons it reads fpl_player_gameweek_history; for the live season, gameweeks whose fpl_fixtures are all finished. Feeds the gameweek picker on /fpl/team-of-the-week. | Page load: /fpl/team-of-the-week (and landing) via src/lib/teamOfWeekApi.ts. |
| `get_countries_by_relevance()` | function | current | yes | no | Lists every country (one row per country) with the number of its teams that appear in at least one match, ordered by that count then name. Powers the Country filter so real leagues (England, Spain...) sort above countries that exist only because one club played a continental-cup tie. | Called on demand by the site (src/lib/referenceApi.ts getCountries, re-exported via lib/api.ts). |
| `get_country_competitiveness()` | function | current | yes | yes | Competitive-balance stats per top-flight league-season (tier 1 leagues plus E0): one row per league_code x season_label with club count, standard deviation of points-per-game, and how often a bottom-half side avoided defeat against a top-half side. Feeds the Countries Compared page. | Called on demand by the site (src/lib/countryInsightsApi.ts -> /football/countries-compared). |
| `get_country_league_summary()` | function | current | yes | yes | Per top-flight league-season summary (tier 1 leagues plus E0; one row per country x league x season): matches, goals per game (total/home/away), H/D/A %, cards per game, over-2.5 %, BTTS %, 0-0 % and comeback % (half-time leader failing to win). Feeds the Countries Compared page. Caveat: card averages exclude matches from '/new/' source files (no card data). | Called on demand by the site (src/lib/countryInsightsApi.ts -> /football/countries-compared). |
| `get_cross_league_summary()` | function | current | yes | yes | Per league-season averages for the English leagues (E0-E3 and EC/National League) with 100+ completed matches: goals per game (total/home/away), H/D/A %, yellows and reds per game, over 2.5 %, BTTS %, 0-0 %, and comeback % (HT leader failed to win). Observed results from public.matches. Used by /football/leagues-compared and a landing tile. | Page load: /football/leagues-compared and landing via src/lib/crossLeagueApi.ts. |
| `get_daily_digest(p_season_id bigint)` | function | current | yes | yes | FPL 'what changed' feed comparing the two most recent snapshot dates: price rises/falls, availability status changes (with news) and ownership moves of 0.5pp or more, one row per change with old/new value, sorted by ownership. Caveat: p_season_id is accepted but NOT used anywhere in the body; snapshots and fpl_players are not season-filtered. Used by /fpl/in-the-papers. | Page load: /fpl/in-the-papers via src/lib/digestApi.ts. |
| `get_digest_gameweeks(p_season_id bigint)` | function | current | yes | no | Lists FPL gameweeks that have day-over-day snapshot pairs (for the 'in the papers' digest), with first/last snapshot date and number of days; each snapshot date is assigned to a gameweek via fpl_gameweek_for_date (last deadline before that date). Caveat: snapshot dates are not season-filtered. Drives the gameweek selector on /fpl/in-the-papers. | Page load: /fpl/in-the-papers via src/lib/digestApi.ts. |
| `get_fpl_default_matchweek(p_season_id bigint, p_league_id bigint)` | function | current | yes | no | Returns the matchweek FPL pages should open on: the next scheduled matchweek if it has leaguewide_v6 projections, else the nearest projected week after it, else the latest projected week, else the next scheduled week, else 1. UI default helper. Frontend calls it with hard-coded p_season_id=13, p_league_id=1. | Page load: FPL gameweek/projection pages via src/lib/fplSeasonApi.ts. |
| `get_fpl_market_movers(p_days integer)` | function | current | yes | yes | FPL market movers over the last p_days (default 7) of snapshots: per player current price, price change, current ownership and ownership change, this gameweek's transfers in/out, status and news, with window start/end dates. Observed FPL data. Used by the market movers panel on /fpl/in-the-papers and the FPL player page. Caveat: joins fpl_players on fpl_player_id only (PK is fpl_player_id+season_id), so it will mis-join once fpl_players holds more than one season. | Page load: MarketMoversPanel (/fpl/in-the-papers) and player page via src/lib/fplMarketApi.ts and fplPlayerPageApi.ts. |
| `get_fpl_optimizer_candidates(p_from_matchweek integer, p_to_matchweek integer)` | function | unused | no | no | Table version of the optimizer candidate feed: rows from fpl_optimizer_candidate_feed_v2 for a matchweek range with price and expected FPL points present. Model projections. | Not called anywhere in the repo (only in generated types). |
| `get_fpl_optimizer_candidates_json(p_from_matchweek integer, p_to_matchweek integer)` | function | unused | no | no | JSON array of optimizer candidate rows (matchweek, player, team, position, price, expected FPL points, start/sub probabilities, expected minutes, home/away, opponent) from fpl_optimizer_candidate_feed_v2 for a matchweek range. Model projections. JSON twin of get_fpl_optimizer_candidates. | Not called anywhere in the repo (only in generated types). |
| `get_fpl_optimizer_candidates_scenario_json(p_season_id bigint, p_league_id bigint, p_model_version text, p_scenario_key text, p_from_matchweek integer, p_to_matchweek integer)` | function | unused | no | no | JSON array of squad-optimizer candidates built directly from fpl_player_projections for a given season, league, model_version and scenario_key over a matchweek range, including a captain_score (xPts weighted by start probability). Model projections; uses current price (now_cost), not historic. | Not called anywhere in the repo (only in generated types). |
| `get_fpl_optimizer_earliest_matchweek()` | function | current | yes | no | Returns the earliest matchweek that has leaguewide_v6 FPL projections (any season). UI helper that bounds the matchweek range on the optimal squad pages. | Page load: /fpl/optimal-squad pages via src/lib/fplOptimizerApi.ts. |
| `get_fpl_projection_snapshot(p_season_id bigint, p_league_id bigint, p_model_version text, p_scenario_key text, p_from_matchweek integer, p_to_matchweek integer)` | function | unused | no | no | Freshness summary of stored FPL projections for a season/league/model/scenario and matchweek range: latest generated_at, row count and matchweeks covered. Diagnostic only. | Not called anywhere in the repo (only in generated types). |
| `get_gameweek_digest(p_season_id bigint, p_gameweek integer)` | function | current | yes | yes | Same change feed as get_daily_digest (price rises/falls, availability changes, ownership moves >= 0.5pp) but for every day-over-day snapshot pair belonging to one FPL gameweek (p_gameweek, default latest), with the date of each change. Observed FPL data. Caveat: snapshots/fpl_players not season-filtered. Used by /fpl/in-the-papers. | Page load: /fpl/in-the-papers via src/lib/digestApi.ts. |
| `get_injury_report(p_season_id bigint)` | function | current | yes | yes | Current FPL injury/availability list: every player whose latest-snapshot status is not 'a' (available), with price, ownership, points, status, chance of playing next round, news, a return date parsed from the news text (heuristic regex, year guessed), number of scheduled fixtures missed before that date and next fixture date. p_season_id (default 13) only scopes the fixture counts. Used by /fpl/injuries, the FPL player page and landing tiles. Caveat: joins fpl_players on fpl_player_id only (PK is fpl_player_id+season_id), so it will mis-join once fpl_players holds more than one season. | Page load: /fpl/injuries, player page and landing via src/lib/injuryApi.ts, fplPlayerPageApi.ts. |
| `get_league_ids_with_results()` | function | current | yes | no | Returns the league_ids that have at least one row in matches, so league/division filters only offer options that lead to real results. | Called on demand by the site (src/lib/referenceApi.ts -> LeagueTable page). |
| `get_market_efficiency(p_bookmaker text, p_closing boolean)` | function | current | yes | yes | Betting-market efficiency by league for 1X2 odds from match_odds (bookmaker default 'Avg', closing prices default true): matches, average overround, and flat-stake ROI % for always backing the favourite, the outsider, home, draw or away. Historic observed odds and results; ROI is a backtest, not advice. Used by /football/market-efficiency. | Page load: /football/market-efficiency and landing via src/lib/marketApi.ts. |
| `get_matchweek_head_to_head(p_league_id bigint, p_season_id bigint, p_matchweek integer)` | function | current | yes | yes | For each fixture in a league/season (optionally one matchweek), historic head-to-head record between the two clubs from public.matches: meetings, wins for this fixture's home side, draws, away-side wins, and the most recent meeting's date/score (with whether the fixture's home side was at home). Meetings are counted across all competitions loaded, not just this league. Used by the gameweek fixtures browser. | Page load: GameweekBrowser (/fixtures) via src/lib/fixturesApi.ts getMatchweekHeadToHead. |
| `get_model_accuracy(p_league_id bigint)` | function | current | yes | yes | Per-fixture model scorecard for played fixtures with stored predictions: model H/D/A probabilities (derived from predicted goals + the fit's rho via fixture_derived_markets), picked outcome, actual result, correct flag, probability given to the actual result and Brier score. Joins fixtures to matches on the natural key (league, season, teams, date). Predictions are the stored point-in-time ones. Used by /football/model-accuracy. | Page load: /football/model-accuracy via src/lib/modelAccuracyApi.ts; also called by get_model_accuracy_summary and get_model_calibration. |
| `get_model_accuracy_summary(p_league_id bigint)` | function | current | yes | yes | One-row scorecard of the Dixon-Coles match model on played fixtures (optional league filter): fixtures scored, correct picks, hit rate %, always-home baseline hit rate, mean Brier score vs uniform baseline 0.6667, and mean probability given to the actual result. Built on get_model_accuracy. Used by /football/model-accuracy and landing tiles. | Page load: /football/model-accuracy and landing via src/lib/modelAccuracyApi.ts. |
| `get_model_calibration(p_league_id bigint)` | function | current | yes | yes | Calibration table for the match model: every H/D/A probability from get_model_accuracy bucketed into 10-point bands (0-10 ... 60+), with number of forecasts, mean predicted %, actual hit rate % and the gap. Used by /football/model-accuracy. | Page load: /football/model-accuracy via src/lib/modelAccuracyApi.ts. |
| `get_model_scorecard()` | function | current | yes | yes | Model-vs-market scorecard: one row per league x season x team_type (established/promoted/relegated/estimated) x season phase, with match count, summed log-loss and Brier score for the model and for de-margined closing market-average odds, predicted vs actual draws and goals. Values are SUMS (divide by n for averages). Reads model_scorecard_matches (refreshed twice daily). | Called on demand by the site (src/lib/scorecardApi.ts -> /football/model-scorecard). |
| `get_model_scorecard_calibration()` | function | current | yes | yes | Calibration table for the Model Scorecard: one row per league x season x outcome (Home/Draw/Away) x probability decile bin, with count, sum of predicted probability and how many actually happened. Reads model_scorecard_matches, so it is only as fresh as that materialized view. | Called on demand by the site (src/lib/scorecardApi.ts -> /football/model-scorecard). |
| `get_model_xi_history(p_season_id bigint, p_league_id bigint)` | function | current | yes | yes | For each finished, data-checked FPL gameweek: the actual Team of the Week points vs the points scored by the model's best XI (by projected points), model XI projected total, overlap count, players projected, and whether projections were generated before the deadline. Feeds the Model XI accuracy page. Caveats: hard-coded to model_version 'leaguewide_v6' baseline; defaults season 13 / league 1; projections may have been generated after the deadline (see generated_before_deadline) so this is not a strict pre-match record -- fpl_projection_snapshots is. | Called on demand by the site (src/lib/modelXiApi.ts -> /fpl/model-xi). |
| `get_model_xi_players(p_event_id integer, p_season_id bigint)` | function | current | yes | yes | The model's best FPL XI for one gameweek (1 GK, 3 DEF, 3 MID, 1 FWD plus the best 3 remaining outfielders by projected points): one row per selected player with projected points, actual FPL points and minutes, and whether he was in the actual Team of the Week. Uses leaguewide_v6 baseline projections; default season 13. | Called on demand by the site (src/lib/modelXiApi.ts -> /fpl/model-xi). |
| `get_most_common_scoreline(p_league_id bigint)` | function | current | yes | yes | Top 4 full-time scorelines for a league across all loaded seasons in public.matches, with occurrences, total matches and seasons covered. Observed results. Used for a trivia tile (landing calls it with the Premier League id). | Page load: football hub trivia via src/lib/landingApi.ts. |
| `get_overround_trend(p_bookmaker text)` | function | current | yes | yes | Average closing 1X2 overround (bookmaker margin) per season and league for a bookmaker (default 'Avg'), league-seasons with 100+ matches only. Observed historic odds. Used by /football/market-efficiency. | Page load: /football/market-efficiency via src/lib/marketApi.ts. |
| `get_player_by_slug(p_slug text)` | function | current | yes | yes | Player profile lookup by player_identity slug: fpl_code, canonical name, latest name/team/position, seasons played, career FPL points and minutes (from fpl_player_season_totals), first/last season, plus current-season stats (points, minutes, goals, assists, bonus, price) if the player is in the current FPL squad (else current_* are null). Used by /fpl/player-scout/:slug. | Page load: player record/scout pages via src/lib/playerScoutApi.ts. |
| `get_player_career(p_fpl_code bigint)` | function | current | yes | yes | Season-by-season FPL career for one player (by fpl_code) from fpl_player_season_totals: team, position, start/end price, points, minutes, goals, assists, clean sheets, bonus, points per start £m, and points split into early (GW1-13), mid (14-26) and late (27+) thirds from gameweek history. Historic completed seasons only (current season is not in season totals). Used on player record/scout pages. | Page load: PlayerCareerRecord / player scout pages via src/lib/playerScoutApi.ts. |
| `get_player_gameweek_breakdown(p_fpl_player_id bigint, p_season_id bigint)` | function | current | yes | yes | Per-gameweek FPL stat line for one player in one season (default 13) from fpl_player_gameweeks: opponent, home/away, kickoff date, minutes, points, goals, assists, CS, conceded, bonus, BPS, saves, cards, own goals, pens missed/saved, defensive contributions, and the leaguewide_v6 projected points generated BEFORE kickoff (null if none pre-kickoff). Takes the season-specific fpl_player_id, not fpl_code. Used on the FPL player page. | Page load: /fpl/players/:slug via src/lib/playerScoutApi.ts. |
| `get_player_season_gameweeks(p_fpl_code bigint, p_season_id bigint)` | function | current | yes | yes | Per-gameweek FPL stat lines for one player (by fpl_code) in one season: live-season rows from fpl_player_gameweeks plus historic rows from fpl_player_gameweek_history (historic rows have no opponent, zero own goals/pens/defcon, and include price). Used on the player record page's season drill-down. | Page load: player record pages via src/lib/playerScoutApi.ts. |
| `get_player_seasons(p_fpl_code bigint)` | function | current | yes | yes | List of seasons a player (by fpl_code) has FPL data for, with season slug, total points and an is_current flag (current season taken from fpl_players). Season picker for player record pages. | Page load: player record pages via src/lib/playerScoutApi.ts. |
| `get_price_change_risk(p_season_id bigint)` | function | current | yes | yes | FPL price-change pressure ranking from the latest snapshot: net transfers this gameweek scaled by ownership into a relative 'pressure' score, with direction rise/fall/steady. Heuristic proxy (not FPL's actual price algorithm) for which players are likely to change price. Caveat: p_season_id is accepted but not used in the body. Used by /fpl/price-risk, player page, landing tiles and list_scout_players. Caveat: joins fpl_players on fpl_player_id only (PK is fpl_player_id+season_id), so it will mis-join once fpl_players holds more than one season. | Page load: /fpl/price-risk, player page, landing via src/lib/priceRiskApi.ts, fplPlayerPageApi.ts, landingApi.ts; also inside list_scout_players. |
| `get_rolling_xi_candidates(p_season_id bigint)` | function | current | yes | no | Current FPL player pool for the season best-XI solver: fpl_code, name, team, position, opening price (now_cost minus cost_change_start), current price, total points and minutes, players with minutes > 0 only. Raw input; the club limit and formation are applied client-side. Used by /fpl/season-xi. | Page load: /fpl/season-xi via src/lib/seasonXiApi.ts. |
| `get_scout_vs_model()` | function | unused | no | no | Compares manually adjusted ('Chief Scout') predicted goals with the raw model's predicted goals on played fixtures where the override changed something: fixtures scored, mean absolute goal error for each, and counts where each was better/tied. Groundwork for a planned Chief Scout feature (OUTSTANDING.md); raw predictions only exist going forward, so it may return few or zero fixtures. | Not called anywhere in the repo (only in generated types). |
| `get_season_player_projections_json(p_matchweek integer)` | function | unused | no | no | JSON array of per-player FPL projection components for one matchweek of the current season: expected minutes, start/sub probabilities, shrunk xG90/xA90, expected goals/assists, clean-sheet probability (exp(-opponent predicted goals)), defensive contribution probability and Monte Carlo expected bonus. Model estimates. Was broken until 2026-09-22 (pointed at a dropped view) and nothing calls it. | Not called anywhere in the repo (generated types only; OUTSTANDING.md notes nothing calls it). |
| `get_set_piece_index(p_season_id bigint)` | function | current | yes | yes | Per-player set-piece value index for a season (default 13): each current duty in set_piece_hierarchies (penalties, direct free kicks, corners; indirect FKs excluded) weighted by the share of goals/assists those set pieces produce (from opta_slot_breakdown) and divided by taker rank, summed per player, with a text summary of duties. Derived heuristic score, not observed output. Used by /fpl/set-pieces and landing. | Page load: /fpl/set-pieces and landing via src/lib/setPieceApi.ts, landingApi.ts. |
| `get_set_piece_takers(p_season_id bigint)` | function | current | yes | yes | Current set-piece taker hierarchies per team for a season (default 13): set-piece type, player name (free text, not an FPL id), rank, confidence, source and last update, only rows still valid. Used by /fpl/set-pieces and the FPL player summary panel. | Page load: /fpl/set-pieces and PlayerSummaryPanel via src/lib/setPieceApi.ts, fplPlayerPageApi.ts. |
| `get_strictest_referees()` | function | unused | no | yes | Top 4 referees (with 50+ matches in public.matches, all leagues/seasons) by total red cards shown, with match counts. Raw totals, not per-game rates. | Not called anywhere in the repo (only in generated types). |
| `get_team_month_profile(p_team_id bigint)` | function | current | yes | yes | A team's historical league record by calendar month and venue: one row per month x home/away with played, points, goals for/against and PPG, ordered in season order (Jul..Jun). Caveat: only English divisions (league_id 1-5) are counted, so non-English clubs return nothing. | Called on demand by the site (src/lib/teamHistoryApi.ts -> TeamHistoryPanel on team pages). |
| `get_team_of_the_week(p_event_id integer, p_season_id bigint)` | function | current | yes | yes | FPL Team of the Week: best XI by actual points for a gameweek (default latest completed) in a 1-3-3-1 minimum plus 3 best remaining outfielders, iteratively excluding the lowest scorer to enforce max 3 per club (loop capped at 8 passes). Historic seasons use fpl_player_gameweek_history; live season uses fpl_player_gameweeks. Observed points. Used by /fpl/team-of-the-week. Caveat: live-season branch joins fpl_players on fpl_player_id only (no season), so it will duplicate rows once fpl_players holds two seasons. | Page load: /fpl/team-of-the-week via src/lib/teamOfWeekApi.ts; also called by get_totw_vs_model. |
| `get_top_actual_fpl_scorer(p_season_id bigint)` | function | current | yes | yes | Top 4 FPL players by actual total points in a season, summed from fpl_player_gameweeks (grouped by web_name, so two players sharing a web_name would merge). Used for an FPL hub landing tile. | Page load: FPL hub landing via src/lib/landingApi.ts. |
| `get_totw_vs_model(p_event_id integer, p_season_id bigint)` | function | current | yes | yes | Compares the actual Team of the Week with the model's preferred XI for the same gameweek (top leaguewide_v6 baseline projected points under the same formation minimums): actual TOTW points, the model XI's actual points and projected points, overlap count and overlapping names. Caveats: the model XI does NOT apply the 3-per-club limit, and it uses whatever projection row is stored now (not filtered to generated before kickoff). Used by /fpl/team-of-the-week and /fpl/model-xi. | Page load: /fpl/team-of-the-week and /fpl/model-xi via src/lib/teamOfWeekApi.ts. |
| `get_xi_weekly_by_codes(p_season_id bigint, p_codes bigint[])` | function | current | yes | yes | Weekly FPL totals for a fixed set of players identified by FPL player code: one row per gameweek with summed points, players who played (minutes > 0) and blanks. Uses fpl_player_gameweek_history for completed seasons and live fpl_player_gameweeks (mapped via fpl_players) for the season in progress. Feeds the Season XI page's week-by-week chart. | Called on demand by the site (src/lib/seasonXiApi.ts -> /fpl/season-xi). |
| `list_scout_players(p_season_id bigint, p_position integer, p_team_id bigint, p_min_minutes integer, p_search text, p_limit integer)` | function | current | yes | yes | Filterable player list for the Player Scout page: current-season FPL players (season default 13) filtered by position (1-4), team_id, minimum minutes and name search, limit 1-700, with price, points, minutes, goals, assists, CS, bonus, ownership, points per £m, seasons played and price-change pressure/direction (from get_price_change_risk). Observed FPL data. | Page load: /fpl/player-scout via src/lib/playerScoutApi.ts. |
| `list_scout_teams(p_season_id bigint)` | function | current | yes | no | Teams in the FPL season (default 13) with player counts, for the team filter on the Player Scout page. | Page load: /fpl/player-scout via src/lib/playerScoutApi.ts (called with season 13). |
| `list_scoutable_players()` | function | unused | no | no | All players with a player_identity slug and historic season totals (minutes > 0), with career FPL points and seasons played, sorted by career points. Probably intended for sitemap/index generation. | Not called anywhere in the repo (only in generated types). |
| `search_players(p_query text, p_limit integer)` | function | current | yes | yes | Name search over player_identity (canonical_name ILIKE, query at least 2 chars, limit 1-50): returns fpl_code, slug, name, latest FPL name/team/position, seasons played, career points (historic season totals), first/last season and current-season slug if in the current squad. Players with no historic season totals (e.g. new this season) are not returned. Used by the Player Scout search box. | Page load: /fpl/player-scout search via src/lib/playerScoutApi.ts. |

## helper

Triggers and small utilities.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `team_slug_history` | table | current | no | no | Previous URL slugs of teams: one row per slug change (team_id, old_slug, replaced_at). Nothing reads it yet (intended for redirects of old team URLs). | Trigger: record_team_slug_change() after update of teams.slug. |
| `fpl_current_season_id()` | function | current | no | no | Returns the season_id of the FPL season currently in play, defined as max(season_id) in fpl_gameweeks; it rolls over automatically when next season's FPL bootstrap is first loaded. Used by many FPL projection views and player functions instead of a hard-coded season. | Called inline by ~20 FPL views/functions (e.g. fixture_player_expected_minutes_resolved_v3_raw, fpl_season_player_projection_feed, get_player_by_slug, search_players) whenever they are queried. |
| `fpl_gameweek_for_date(p_season_id bigint, p_date date)` | function | current | no | no | Returns the FPL gameweek (fpl_event_id) a calendar date belongs to for a season: the latest gameweek whose deadline (Europe/London date) is before that date. SECURITY DEFINER, anon-executable. | Called inside get_digest_gameweeks and get_gameweek_digest (page load of /fpl/in-the-papers). |
| `generate_fixture_slug()` | function | current | no | no | Trigger function: BEFORE INSERT on fixtures, fills a null slug as '<home team slug>-v-<away team slug>-<kickoff_date>'; raises if either team has no slug. | Trigger fixtures_generate_slug (BEFORE INSERT ON fixtures). |
| `generate_fpl_player_slug()` | function | current | no | no | Trigger function: on fpl_players insert or name change, sets slug from slugify(first + second name, else web_name), keeping the existing slug if the name is unchanged and adding -2, -3 ... to stay unique within the season. | Trigger fpl_players_generate_slug (BEFORE INSERT OR UPDATE OF first_name, second_name, web_name ON fpl_players). |
| `record_team_slug_change()` | function | current | no | no | Trigger function: when a team's slug changes, inserts the old slug into team_slug_history (so old URLs can redirect). | Trigger team_slug_change_history (AFTER UPDATE OF slug ON teams). |
| `refuse_snapshot_changes()` | function | current | no | no | Trigger function that makes FPL projection snapshot tables append-only: any UPDATE or DELETE raises an exception. | Fires BEFORE UPDATE OR DELETE on fpl_projection_snapshots and fpl_projection_fixture_snapshots. |
| `safe_numeric(p_text text)` | function | current | no | no | Casts text to numeric if it looks like a plain decimal number, otherwise returns NULL (no exception). Import helper. | Called inside backfill_match_odds(). |
| `set_current_season_id()` | function | current | no | no | Trigger function: BEFORE INSERT on FPL tables, fills a NULL season_id with the season whose 1 Aug-1 Aug window contains today's date. Safety net; refresh_fpl now sets season_id itself. | Triggers fpl_players_season_id_default, fpl_player_gameweeks_season_id_default, fpl_player_snapshots_season_id_default (BEFORE INSERT). |
| `set_fpl_code_from_payload()` | function | current | no | no | Trigger function: on fpl_players insert/update, fills a NULL fpl_code from source_payload->>'code' (FPL's stable cross-season player code). | Trigger fpl_players_code_default (BEFORE INSERT OR UPDATE ON fpl_players). |
| `set_player_identity_slug()` | function | current | no | no | Trigger function on player_identity that assigns a unique URL slug from canonical_name (slugify_player_name, falling back to 'player-<fpl_code>', with -2, -3... suffixes on collision). Leaves an existing slug alone unless the name changes; respects a caller-supplied slug. | Fires BEFORE INSERT OR UPDATE on player_identity (trigger player_identity_slug), e.g. during sync_player_identity(). |
| `slugify(input text)` | function | current | no | no | Generic URL slug maker (public.unaccent, lowercase, non-alphanumerics to '-', collapse and trim dashes; NULL if empty). Near-duplicate of slugify_player_name(). | Called inside generate_fpl_player_slug() trigger. |
| `slugify_player_name(p_name text)` | function | current | no | no | Turns a player name into a URL slug (unaccent, lowercase, non-alphanumerics to '-', collapse and trim dashes; NULL if empty). Functionally the same as slugify() but calls unaccent without schema qualification. | Called inside sync_player_identity() and set_player_identity_slug(). |
| `sync_player_identity()` | function | current | no | no | Trigger function (AFTER INSERT OR UPDATE on fpl_players) that keeps player_identity in step: creates the cross-season identity row keyed on fpl_code with a canonical name and a stable slug (collisions get '-<fpl_code>' appended), and on later runs only widens first/last-seen season, never changing an existing slug. | Fires automatically on every fpl_players write, i.e. each private.refresh_fpl() run (pg_cron fpl-refresh-6-hourly, 17 */6 * * *). |

## admin

Admin-only checks, audits and tools.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `admin_bootstrap_emails` | table | current | no | no | Allowlist of email addresses permitted to create an account; one row per email. handle_new_auth_user() marks a matching sign-up as admin in app_users, and (per OUTSTANDING.md) a BEFORE INSERT trigger on auth.users rejects any email not listed here. Security config only — contains the owner's email. | Manual: add an email via SQL before that person signs up. |
| `app_users` | table | current | no | no | One row per signed-in Supabase auth user with an is_admin flag; the source of truth for public.is_admin(), which gates every admin write policy and admin page. Contains user emails — internal only. | Trigger: handle_new_auth_user() on auth.users insert (admin flag from admin_bootstrap_emails). |
| `data_health` | view | unused | no | no | Four-row pipeline health summary (one row per component: Fixtures, League Results, Predictions, Model Fit) showing last attempt/success, a Healthy/Warning/Stale label and row counts, built from fixture_refresh_runs, result_ingestion_runs, fixtures and model_fit_runs. Nothing in src/, scripts or other definitions reads it (the /data-health page uses other sources such as league_fit_status); treat as an unused admin diagnostic. |  |
| `fpl_full_season_projection_health_v1` | view | unused | no | no | Coverage check for the live FPL projections: one row per matchweek of the current season (league_id = 1, i.e. Premier League) with fixture count, fixtures with team goal predictions, fixtures with leaguewide_v6 player projections, projection row count and a full_player_coverage flag. Signed-in/service only; nothing in src/, scripts or other definitions reads it (mentioned only in OUTSTANDING.md). |  |
| `meta_flow_edges` | table | current | no | no | Data-lineage graph for the catalogue: one row per parent_key -> child_key dependency between tables/views/functions (source = catalog/parsed/manual). Admin-only; read via meta_flow_summary on the Data Flow page. | Re-derived by public.meta_refresh_flow() (pg_cron meta-refresh-flow-daily, 05:45 UTC) or on demand from the Data Flow page. |
| `meta_flow_history` | table | current | no | no | Change history for the data catalogue: one row per event on a node (added, definition changed, disappeared, returned, commentary). Admin-only Data Flow page timeline. | Written by meta_refresh_flow() (pg_cron meta-refresh-flow-daily, 05:45 UTC) and trigger meta_flow_note_commentary() when notes are edited. |
| `meta_flow_nodes` | table | current | no | no | The data catalogue itself: one row per table/view/matview/function with layer, human-written purpose/refresh_note/commentary, definition hash, row estimate and presence. Admin-only (Data Flow page). row_estimate comes from planner stats and is unreliable (shows 0 for populated tables such as countries). | Structure re-derived by meta_refresh_flow() (pg_cron meta-refresh-flow-daily, 05:45 UTC); human columns edited on the Data Flow page and never overwritten. |
| `team_tactical_review_log` | table | current | yes | no | When an admin last reviewed each team's tactical roles: one row per season + team. Workflow bookkeeping for the Tactical Roles page (also loaded, unused visibly, by the public Starting Lineups view). | Admin 'mark reviewed' on the Tactical Roles page. |
| `_check_auth_user_token_nulls_impl()` | function | current | no | no | Internal, SECURITY DEFINER: counts auth.users rows with any NULL token/change column (confirmation, recovery, email/phone change, reauthentication), a state that makes Supabase sign-in return 500 for everyone. Not directly callable by clients; reached through check_auth_user_token_nulls(). | Called only via check_auth_user_token_nulls() (and so by the data integrity report). |
| `_get_data_integrity_report_impl()` | function | current | no | no | Internal body of get_data_integrity_report(): runs the integrity checks (public read access, FPL refresh freshness within 9h, refresh_fpl failures in cron.job_run_details over 48h, auth token NULLs, NULL season_id in six FPL tables, gameweek history reconciliation via check_fpl_history_integrity, stored season XIs legal, historic player identities) and returns check_name/status/detail. EXECUTE revoked from all client roles. | Called only by get_data_integrity_report() (admin, /data-health). |
| `_get_public_read_audit_impl()` | function | current | no | no | Internal body of get_public_read_audit(): reads pg_class, pg_policies and role grants to report, for every public table/view/matview, RLS status, SELECT policy, anon SELECT grant and effective anon readability. EXECUTE revoked from all client roles. | Called only by get_public_read_audit(). |
| `_require_admin()` | function | current | no | no | Guard: raises 'admin only' (SQLSTATE 42501) unless is_admin() is true for the caller or the JWT role is service_role. Called at the top of admin-only SECURITY DEFINER wrappers. | Called inside check_auth_user_token_nulls(), get_data_integrity_report(), get_public_read_audit(). |
| `check_auth_user_token_nulls()` | function | current | no | no | Admin-only (SECURITY DEFINER + _require_admin) wrapper returning bad_rows = number of auth.users with NULL token columns (should be 0; non-zero breaks sign-in). | Called by _get_data_integrity_report_impl (i.e. when /data-health is opened). Not scheduled on its own. |
| `check_fpl_history_integrity(p_season_id bigint)` | function | current | no | no | Reconciliation check for imported historic FPL data: per season, sums fpl_player_gameweek_history by player and compares with fpl_player_season_totals, returning players checked, mismatch counts for points/minutes/goals/assists and the worst points gap. Non-zero means the import lost or duplicated rows. | Called by _get_data_integrity_report_impl for every season with history; run manually after imports. |
| `check_model_integrity()` | function | current | no | no | Daily guard suite returning (check_name, status, found, detail) rows, one per past incident: point-in-time prediction checks on fixtures and match_predictions, recent-fit scoring level, fit versioning and retro-fit stamps, service_role and anon read grants, stale scheduled/postponed fixtures, cup ingestion freshness, stuck pipeline runs and duplicate function names. Read-only; any 'failed' row fails the workflow. | Called by scripts/run_integrity_checks.py in the 'Model integrity checks' GitHub workflow (cron 45 9 * * *, 09:45 UTC, plus manual). |
| `get_data_integrity_report()` | function | current | no | no | Admin-only (SECURITY DEFINER, raises 'admin only' unless is_admin() or service_role) data health report: returns check_name/status/detail rows for public-read access, FPL refresh freshness and cron-logged failures, auth token NULLs, NULL season_id across FPL tables, gameweek-history reconciliation, legal stored XIs and historic player identities. Thin wrapper over _get_data_integrity_report_impl(). Shown on /data-health. | On demand: /data-health page (admin) via src/lib/healthApi.ts. Not scheduled. |
| `get_public_read_audit()` | function | current | no | no | Admin-only (SECURITY DEFINER + _require_admin) audit of every public table/view/matview: RLS enabled, has a SELECT policy, anon has SELECT grant, and whether anon can actually read it. Flags objects granted to anon but blocked by RLS (pages would render blank). Wrapper over _get_public_read_audit_impl(). | On demand: /data-health (admin) via src/lib/healthApi.ts; also called by _get_data_integrity_report_impl. |
| `get_tactical_role_worklist(p_season_id bigint)` | function | current | no | no | Admin worklist of outfield players whose tactical role is still the 'fpl_position_fallback' default in team_player_tactical_defaults, with minutes, ownership, points and a starter/rotation/fringe priority, so an admin can assign real roles. Note: EXECUTE is granted to anon/PUBLIC even though it serves an admin page. | On demand: /fpl/tactical-roles admin page via src/lib/tacticalRoleAdminApi.ts. |
| `get_unmapped_alias_names(p_source text, p_raw_names text[])` | function | current | no | no | Import helper: given a data source name and a list of raw team names from a fetched file, returns only the names with no team_aliases row for that source yet, so only genuinely new clubs need manual mapping when adding a season or league. | Called manually (SQL / service_role) during new season or league imports; no code in the repo calls it (OUTSTANDING.md notes it was not needed for the last import pass). |
| `handle_new_auth_user()` | function | current | no | no | Trigger function (SECURITY DEFINER) for new Supabase auth users: upserts an app_users row with email and is_admin=true if the email (case-insensitive) is in admin_bootstrap_emails; never downgrades an existing admin. Unclear which trigger binds it: no CREATE TRIGGER for it in the repo (it would live on auth.users, which the schema export does not include). | Trigger on auth.users sign-up (binding not in repo; inferred from body). |
| `is_admin()` | function | current | no | no | Returns true if the current auth user (auth.uid()) has is_admin in app_users. SECURITY DEFINER; used by RLS policies, admin guards and the frontend to show admin UI. | Called on sign-in/session restore by src/lib/auth.tsx, by netlify/functions/trigger-workflow.ts, and inside _require_admin, meta_refresh_flow and RLS policies. |
| `meta_refresh_flow()` | function | current | no | no | Re-derives the data-flow registry (meta_flow_nodes / meta_flow_edges / meta_flow_history) from the Postgres catalogue: fingerprints every public table/view/function, records added/changed/disappeared/returned objects, and rebuilds non-manual edges from view dependencies and by regex-scanning function bodies. Human columns (layer, purpose, notes) are never overwritten. SECURITY DEFINER; intended admin/cron only (but see guard caveat: the current_user check is always true inside SECURITY DEFINER). | pg_cron job meta-refresh-flow-daily at 05:45 UTC; also on demand from the admin 'refresh' button on /data-flow (src/lib/metaFlowApi.ts). |

## meta

This catalogue and the schema-export machinery.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `meta_catalogue_gaps` | view | current | no | no | Catalogue to-do list: one row per live, non-scratch table/view/function that has no purpose text, or whose definition changed (meta_flow_history) after its entry was last reviewed (purpose_reviewed_at). Read by check_model_integrity() as the 'catalogue_current' warning. Admin-only via RLS on meta_flow_nodes. |  |
| `meta_flow_summary` | view | current | no | no | Data-catalogue summary for the admin Data Flow page: one row per meta_flow_nodes object (table/view/function) with layer, purpose, refresh note, commentary, row estimate, counts and list of upstream/downstream edges, and definition-change history counts. |  |
| `export_schema_definitions()` | function | current | no | no | Returns (kind, name, definition) for every non-extension view, materialized view and function in the public schema, as CREATE statements. It is the source of the repo's supabase/definitions/ files that this catalogue relies on. | Called by scripts/export_db_definitions.py in the 'Export database definitions' GitHub workflow (cron 35 5 * * 1, Mondays 05:35 UTC, plus manual), which opens a PR when anything changed; service_role only. |
| `meta_flow_note_commentary()` | function | current | no | no | Trigger function on meta_flow_nodes (AFTER UPDATE): when a node's layer, purpose, refresh_note or commentary changes, writes a 'commentary' entry to meta_flow_history with the editor's email. Errors if called directly. | Fires automatically on UPDATE of meta_flow_nodes (trigger meta_flow_commentary_history), e.g. edits from the admin Data Flow page. |
| `meta_flow_stamp_review()` | function | current | no | no | Trigger function on meta_flow_nodes: stamps purpose_reviewed_at = now() whenever purpose, refresh_note or status changes (unless the same update sets purpose_reviewed_at explicitly). Feeds the catalogue staleness check. | BEFORE UPDATE trigger meta_flow_stamp_review on meta_flow_nodes. |

## scratch

Backups, superseded versions and unused objects: do not build on these.

| Object | Kind | Status | Public | AI | Purpose | Refresh |
|---|---|---|---|---|---|---|
| `backup_fpl_fixtures_20260924` | table | unused | no | no | Point-in-time backup copy taken before a key/schema change on 20260924. Not read by anything; safe to drop once the change is confirmed. | One-off; not refreshed. |
| `backup_fpl_gameweeks_20260924` | table | unused | no | no | Point-in-time backup copy taken before a key/schema change on 20260924. Not read by anything; safe to drop once the change is confirmed. | One-off; not refreshed. |
| `backup_fpl_player_gameweeks_20260924` | table | unused | no | no | Point-in-time backup copy taken before a key/schema change on 20260924. Not read by anything; safe to drop once the change is confirmed. | One-off; not refreshed. |
| `backup_fpl_player_projections_gw4_20260922` | table | unused | no | no | Point-in-time backup copy taken before a key/schema change on 20260922. Not read by anything; safe to drop once the change is confirmed. | One-off; not refreshed. |
| `backup_fpl_teams_20260924` | table | unused | no | no | Point-in-time backup copy taken before a key/schema change on 20260924. Not read by anything; safe to drop once the change is confirmed. | One-off; not refreshed. |
| `data_source_competitions` | table | unused | no | no | Intended registry of external data-source competitions (one row per source + competition + season, with URL, enabled flag and priority). Nothing reads or writes it: the raw backfill edge functions set source_match_rows.source_competition_id to NULL and imports hard-code their URLs. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `fixture_actual_team_lineups` | table | unused | no | no | Observed formation per fixture and team (one row per fixture + team, with source and an is-inferred flag). Nothing in the repo or other definitions reads or writes it (only generated types). | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `fixture_player_expected_minutes` | view | superseded | no | no | Original (v1) expected-minutes model: one row per fixture x team x FPL player, from the lineup-source consensus plus squad hierarchy, scaled so each team sums to 990 minutes. Superseded by fixture_player_expected_minutes_v2/_v3 and the resolved_v3 chain; nothing in the repo or other definitions reads it. Do not use. |  |
| `fpl_optimizer_candidate_feed_v1` | view | superseded | no | no | Old FPL squad-optimiser candidate list (one row per fixture x player with matchweek, price and xPts) built on fpl_projection_frontend_feed, which only returns retired 'leaguewide_v4' projections. Superseded by fpl_optimizer_candidate_feed_v2; nothing reads it. Do not use. |  |
| `fpl_projection_data_health` | view | superseded | no | no | Per-fixture readiness label (ready / awaiting_player_projection / incomplete) for current-season E0 fixtures, built on fpl_projection_fixture_readiness. Because that view counts only retired 'prototype_v3' projections, the status is wrong for the live leaguewide_v6 model. Nothing reads it; superseded by fpl_full_season_projection_health_v1. |  |
| `fpl_projection_fixture_readiness` | view | superseded | no | no | Per current-season E0 fixture: whether team goal predictions exist, number of lineup-prediction sources and player rows, and count of fpl_player_projections rows, but only for model_version 'prototype_v3' (retired), so projection_rows is stale for the live leaguewide_v6 model. Server-only; read only by fpl_projection_data_health. Superseded by fpl_full_season_projection_health_v1. |  |
| `fpl_projection_frontend_feed` | view | superseded | no | no | Old frontend projection feed filtered to model_version 'leaguewide_v4', which src/lib/fplApi.ts calls stale and not to be used. Superseded by fpl_projection_frontend_feed_v6; only the also-superseded fpl_optimizer_candidate_feed_v1 reads it. Do not use. |  |
| `ingest_finance_stage_tmp` | table | unused | no | no | Temporary staging table used during a manual finance ingestion load (club_slug, year, period dates, raw JSON). Not read by the site; owned by the finance ingestion workstream. | One-off; not refreshed. |
| `opta_player_match` | table | unused | no | no | Per-player-per-match Opta rows (2011/12 workbook: formation slot, minutes, goals/assists by set-piece type). Nothing reads or writes it; scripts/extract_opta_workbook.py deliberately aggregates to opta_slot_breakdown instead of storing player rows. Historic 2011/12 data — no current players. | None (only generated types reference it). |
| `player_availability_events` | table | unused | no | no | Intended time series of FPL availability/injury news snapshots (one row per player observation: status, chance of playing, news, expected return). Nothing in the repo or definitions reads or writes it; a backup_player_availability_events_20260919 copy exists in the schema. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `player_lineup_predictions` | table | unused | no | no | Intended per-fixture player start/minutes predictions (prob_start, prob_60_plus, expected minutes, model_version). Nothing reads or writes it; FPL projections use fpl_player_projections instead. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `player_match_roles` | table | unused | no | no | Intended per-fixture observed player roles (started, position, tactical role, formation slot, minutes). Nothing reads or writes it (only generated types). | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `projection_scenario_players` | table | unused | no | no | Player overrides within a user projection scenario (start, minutes, role, set-piece ranks). Part of an unbuilt multi-user 'scenarios' feature; nothing reads or writes it. | None. |
| `projection_scenario_teams` | table | unused | no | no | Team formation overrides within a user projection scenario. Part of the unbuilt scenarios feature; nothing reads or writes it. | None. |
| `projection_scenarios` | table | unused | no | no | Header for a named user projection scenario per fixture. OUTSTANDING.md notes the feature was deliberately not used (raw_predicted_* columns on fixtures chosen instead); table kept in case scenarios become real. | None. |
| `tactical_player_match_observations` | table | unused | no | no | Intended per-player-per-match tactical observations (position, slot, canonical role, stats) under tactical_team_match_observations. Nothing reads or writes it. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `tactical_team_match_observations` | table | unused | no | no | Intended per-team-per-match formation observations from external tactical sources. Nothing reads or writes it. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `team_categories` | table | unused | no | no | Named team groupings (e.g. 'Big Six', 'Newly Promoted') with description and colour; one row per category. getTeamCategories() exists in src/lib/referenceApi.ts but no page calls it. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `team_category_memberships` | table | unused | no | no | Which teams belong to each team category in each season; one row per category + team + season. Only read by the uncalled getTeamsInCategory() in src/lib/referenceApi.ts. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `team_match_tactics` | table | unused | no | no | Intended per-fixture observed team formation with source and confidence. Nothing reads or writes it. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |
| `team_strength_forward_adjustments` | table | unused | no | no | Intended forward-looking attack/defence log adjustments per season + team with effective dates, decay and scenario key. Nothing reads or writes it; team_strength_manual_override is the live mechanism. | No writer in the repo (no script, workflow, edge function or DB function inserts into it); populated ad hoc by manual SQL/one-off loads. |

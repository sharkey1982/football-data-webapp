# Internal tools and raw/admin data views -- not stable public content,
# not meant to be indexed. See the review notes in the project memory for
# the reasoning (raw source-data layer, personal optimizer output, admin
# tactical-role editing, internal data-health monitoring).
User-agent: *
# Results Data is deliberately NOT disallowed: it moved into
# Football > Discover as the public, filterable match archive.
#
# The optimiser pages are no longer disallowed either. They were blocked
# when they were personal output behind the admin menu; they're now
# public features -- Optimiser under Predict, Squad of the Season under
# Discover -- and blocking a page that's linked from the main nav is
# both contradictory and a waste of two real content pages.
#
# What remains blocked is operational: raw provider files, pipeline
# health, and admin-only configuration.
Disallow: /source-data
Disallow: /data-health
Disallow: /fpl/tactical-roles
# The admin sign-in page. Nothing to index, and an indexed login page is
# an invitation to credential-stuffing traffic.
Disallow: /login

Sitemap: https://footballdatashark.netlify.app/sitemap.xml
